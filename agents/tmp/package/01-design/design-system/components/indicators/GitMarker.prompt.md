Single-letter Git status glyph shown at the right edge of a changed file row in the explorer. Just the colored letter — no chip, no background.

```jsx
<GitMarker status="M" />  {/* modified — amber */}
<GitMarker status="A" />  {/* added — green */}
<GitMarker status="D" />  {/* deleted — red */}
```
