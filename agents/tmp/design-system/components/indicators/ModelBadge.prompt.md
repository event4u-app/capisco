Tiny label naming the model behind an agent session. Keep `neutral` (gray) almost everywhere; `accent` only to spotlight the focused session.

```jsx
<ModelBadge>Claude</ModelBadge>
<ModelBadge>GPT-5</ModelBadge>
<ModelBadge tone="accent">Local</ModelBadge>
```
