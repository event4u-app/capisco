# Capisco App — Decision Gates

Foundation-Entscheidungen mit gesetzten Defaults (Quelle:
`agents/roadmaps/00-capisco-overview.md` §3). **Override:** hier ändern + Begründung.

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Shell-Träger | **Vite-only Browser-App** (Tauri-Wrapper später) | angewendet | Track UI-Shell; maximal autonom verifizierbar. „Desktop"-Verhalten kommt hinter ein `DesktopShell`-Interface (noch nicht nötig). |
| Komponenten-Quelle | **`_shadcn/` kanonisch** → `src/components/{ui,capisco}` | angewendet | build-plan §1. In-Browser-`components/` ist nur Mock-Spiegel. |
| Tailwind | **v3** (`^3.4`), HSL-CSS-Vars | angewendet | Gelieferte `tailwind.config.ts` ist v3-Stil. |
| Theme | **Dark kanonisch** (`.dark`), Light-Toggle, `localStorage` | angewendet | Konzept §5.6. `src/lib/theme.tsx`. |
| Fonts | **self-hosted** via `@fontsource/{inter,jetbrains-mono}` | angewendet | build-plan §5; kein CDN (Determinismus für Harness). |
| Icons | **Lucide** als npm-Dep, gebündelt (`src/components/icon.tsx`) | angewendet | Design-Readme; kein CDN-Inject. |
| Brand-Mark | **Platzhalter** `src/assets/capisco-mark*.svg` | angewendet | build-plan §5; echtes Asset später. |
| i18n | **react-i18next**, `en` default, ab Tag 1 | angewendet | Konzept §5.9. `src/i18n/`. |
| Overdue-Schwelle | **7 Tage, konfigurierbar** | angewendet (R5) | build-plan §3-Korrektur (nicht 3). `mockGitProvider.overdueThresholdDays = 7`; Overdue-Tab + PR-`Nd ready`-Badge gegen diesen Wert (Prototyp hartkodierte `> 3` — korrigiert). UI-Selektor in der Filterleiste des Overdue-Tabs (Default 7). |
| Chart-Farb-Tokens (R5) | **Eigener `--chart-*`-Block in `globals.css`** (HSL-Tripel, light+dark), konsumiert via `hsl(var(--chart-line))` etc. | angewendet | Der Prototyp nutzt rohe `var(--accent)`/`var(--syn-*)`-Tokens aus `tokens/colors.css`+`tokens/syntax.css`, die **nicht** in die App importiert werden (nur `globals.css` lädt main.tsx). Charts dürfen daher diese rohen Tokens nicht referenzieren. Stattdessen ein gethemter Chart-Palette-Block (`--chart-1..6`, `--chart-line`, `--chart-ideal`, `--chart-grid`, `--chart-good`/`--chart-bad` für die Heatmap), abgeleitet aus den existierenden Rollen. Kein hartcodiertes Hex in Komponenten. |
| Git/Tasks-Datenform (R5) | **`mockGitProvider` + `mockTasksProvider`** hinter den R0-Interfaces (`PullRequest`/`Metric`/`AwarenessEntry`/`WorkHeatmap`/`Ticket`/`Epic`/`Sprint`/`BurndownSeries`), ergänzt um `WeeklySeries`/`DonutSegment`/`LangStat`/`WipRow`/`TicketColumn` für die Chart-Eingaben | angewendet | R0 nannte die Kern-Shapes; die Chart-Serien (Weekly-Line-Daten, Donut-Segmente, Sprach-Breakdown, Team-WIP, Throughput) brauchen eigene deterministische Eingabe-Shapes. Alle Werte aus `shared.jsx`, deterministisch (kein Date.now/Math.random). |
| Git/Tasks-Center-Workspace (R5) | **Eigene Center-Workspaces** (`shell/git/GitWorkspace`, `shell/tasks/TasksWorkspace`), gemountet in `Shell` für `mode==="git"`/`"tasks"` (vorher Placeholder) | angewendet | Prototyp `GitWorkspace`/`TaskDashboard` sind Center-Flächen (volle Breite), keine Left-Panels. Der `mode`-Switch der ActivityBar existiert bereits; nur das Rendering fehlte. |
| Ticket-Detail-Tabs (R5) | **Lokaler `useState` im `TasksWorkspace`** (offene Ticket-IDs + aktiver Tab), schließbar | angewendet | Prototyp `TaskDashboard` hält offene Tickets lokal; kein globaler Store nötig (Tabs sind workspace-lokal, überleben keinen Reload — bewusst, wie der Prototyp). |
| Tooling-Datenform (R6) | **`mockContainerGroups` + `mockDatasources` + `mockSignalProvider`** hinter den R0-Interfaces (`ContainerGroup`/`ServiceStat`, `Datasource`/`DbTable`, `SignalItem`), ergänzt um `SignalSource`/`SignalRule`/`SignalProvider` für die geteilte Signal-Fläche | angewendet (R6) | R0 nannte die Kern-Shapes; §5.2 verlangt *eine* Schiene, die PR/Container/Observability/Agent/Lint in **eine** `SignalItem`-Form faltet — dafür die `source`-Achse + eine bewusst dumme Regel-Liste (`SignalRule[]`, 5 Regeln) + ein `signalsFor(channel)`-Selektor. Alle Werte deterministisch aus `shared.jsx` (kein Date.now/Math.random). |
| Datasource-Credential (R6) | **`credentialRef`-Feld** (Referenzname, z.B. `prod-readonly`), nie ein Wert | angewendet (R6) | Invariante §2.1 „Secrets nie als Wert" — der Prototyp zeigte gar kein Credential; die App surface es als Referenz (`credential: prod-readonly`). Test verifiziert, dass der Wert keine Secret-Form (`:`/`=`/`password`/`token`) hat. |
| Flyout-Pin (R6) | **`pinnedFlyouts: string[]` im Layout-Store** (+ `togglePin`, `FLYOUT_TOOL_IDS`, `useActiveOverlayFlyout`); unpinned = Overlay über dem Center (kein Grid-Shrink, `onClickCapture` im Workspace schließt), pinned = normale rechte PanelStack-Spalte (Center schrumpft auf 340px) | angewendet (R6) | §2: „pinned dockt als Spalte, Center schrumpft; unpinned overlay schließt bei Klick in Workspace". Der Prototyp rendert Alerts/Inspect nur als normale Rechts-Panels — Pin/Overlay ist die hier verankerte Konzept-Fläche. Pin-Toggle in der Flyout-Kopfzeile + Command-Palette (`pin:*`), persistiert (Store-Version 3). |
| Services-Rail-Default (R6) | **`services` zur `DEFAULT_GROUPS.leftTop`** ergänzt | angewendet (R6) | Der Tool war im Store nie im Default-Rail gedockt (Prototyp `leftTop` enthält ihn); ohne Eintrag gab es keinen `rail-item-services`. |
| Chat-Lesespalte | **zentriert ~740px** | angewendet (Design-Sync P0) | build-plan §3-Korrektur. `Transcript` rendert jede Zeile in `mx-auto max-w-[740px]`; settled-Computed-Width-Assert (`getComputedStyle(...).maxWidth === "740px"`) im Visual-Gate + DOM-Assert im Unit-Test. Schließt das offene R2-Gate. |
| Editor-Lib | **CodeMirror 6** (read-only, Mock-Doc) | angewendet (R3) | Konzept §5.6.6 „reife Basis erben". CM6 deps: `@codemirror/{state,view,language,lang-javascript,commands}` + `@lezer/highlight`. |
| Editor-Tab-Reihen (Design-Sync P1) | **Eigener persistenter `useTabRows`-Store** (`shell/editor/tab-rows-store.ts`, `rows: 1\|2\|3`, `localStorage: capisco-tabrows`), getrennt vom (unpersistenten) `useEditor`-Store | angewendet | PhpStorm-Stil mehrreihig/Overflow/Swipe. Default 1 (kein gespeicherter Wert) → CI/Goldens unverändert vom Default-Boot; `rows≥2` bricht via `flex-wrap` um, gecappt auf `rows*32px` (Tab-Höhe `h-8`), darüber vertikal scrollend. Overflow-Dropdown (Chevron rechts) listet alle Tabs (Pin/Dirty-Marker, Klick springt) + hostet das 1/2/3-Segment im Header. Eigener Store, weil der Editor-Store bewusst session-ephemer ist; nur die Reihen-Wahl persistiert (Prototyp-Schlüssel verbatim). |
| Editor-Tab-Beispielmenge (Design-Sync P1) | **Mock-`DOCS` von 3 → 9** (führende broker/worktree/types stabil; 6 Stub-Tabs angehängt) | angewendet | Overflow + Mehrreihigkeit brauchen sichtbares Überlaufen (Prototyp 9 Tabs). Führende 3 behalten ihre Rollen (broker pinned+aktiv, worktree dirty, types Rename-Ziel) → bestehende Tests/Goldens-Intent bleiben; angehängte Tabs sind echte öffenbare Docs (leichter Stub-Text, keine Provider-Overlays). Datei-Tabs **ungetrimmt** (`whitespace-nowrap`, kein `truncate`). **Editor-Goldens neu generiert** (Chevron + zusätzliche Tabs). |
| Editor-Theme (R3) | **Eigene CM6-Extension** (`cm-theme.ts` + `cm-extensions.ts`), Werte aus den CSS-Token-Vars (`--syn-*`, `--bracket-*`, `--indent-guide*`) | angewendet | Prototyp-`.t-line`-CSS lässt sich nicht auf CM6 anwenden; CM6-Theme referenziert die Live-CSS-Vars → Light-Theme invertiert gratis. Rainbow-Brackets + Indent-Guides als ViewPlugins, Git-Change-Bars als eigener Gutter. |
| Editor-Datenform (R3) | **`EditorDoc`** (`{file, ext, text, pinned?, dirty?}`) + Provider-Outputs `ChangeBar`/`FoldRange` ergänzt zu R0-`editor.ts` | angewendet | R0-Interfaces nannten nur die vier Provider-Output-Shapes; CM6 braucht den rohen Doc-Text (das Einzige, was CM6 indiziert) plus Git-Change-Bars und Fold-Ranges als **Provider**-Output (Council-P1: aus dem Mock, nicht aus CM6-Syntax). `PresenceMarker` um `init` + `diff[]` erweitert (Avatar-Initialen + Inline-Diff fürs Live-Popup). |
| Code-Folding-Quelle (R3) | **`foldService` aus den Mock-`FoldRange[]`**, nicht auto-collapsed beim Mount | angewendet | Roadmap: „Code-Folding (Ranges aus Mock-Provider)". Ein `foldService` macht die Provider-Ranges faltbar (Chevrons im Fold-Gutter); Auto-Collapse beim Mount würde den Showcase-Code (Prototyp zeigt die volle Datei) verstecken. `javascript()` trägt zusätzliche Syntax-Folds bei — Folds sind also ≥ die 2 Provider-Ranges. |
| Provider-Output-Rendering (R3) | **React-Overlays über der CM6-Sicht** (Autocomplete/Inlay/Blame/Social-Lane), positioniert aus den Mock-Zeilendaten | angewendet | Council-P1: das sind LSP-/Git-/Presence-*Outputs*, keine CM6-Features. Robustes, ehrliches Rendering aus den Mock-Zeilennummern statt pixel-genauem Ankleben an CM6-Glyph-Spalten (Akzeptanz = DOM/testid-Assert + Sicht-Abnahme). |
| Reduced-motion-Caret (R3) | **JS-Hook (`useReducedMotion`) flippt `data-reduced` + droppt die Blink-Animation** | angewendet | Playwrights `reducedMotion:reduce`-Config emuliert die Media-Query in diesem Chromium **nicht** für `matchMedia`/computed-styles (nur fürs Screenshot-Freezing). Der Test emuliert daher explizit via `page.emulateMedia({reducedMotion:"reduce"})` und prüft den Flip. Die globale `globals.css`-Reduced-motion-Regel bleibt zusätzlicher CSS-Fallback. |
| Default-Shell-Layout (R1) | **Kein Panel offen** (zentrierter Placeholder); Rail-Klick öffnet Panel | angewendet | Hält den Phase-0-Golden `shell-dark.png` gültig; Prototyp öffnet zwar Explorer default, aber „Placeholder behalten" (R1-Assignment) wiegt schwerer. |
| Diff-Datenform (R1) | **`DiffDoc`** (`{file, ext, added, removed, rows: DiffRow[]}`), abgeleitet aus `DiffStat` | angewendet | R0-Interfaces nannten nur `DiffStat`/`ChangeSet`; die Side-by-side-Zeilenstruktur (`shared.jsx` `DIFF`) braucht eine eigene Zeilenform. UI baut gegen `DiffDoc`, Mock `mockDiff` implementiert es. |
| Virtualisierung (R1) | **Eigenes `VirtualList`-Primitiv** (fixed-row windowing, keine neue Dep) | angewendet | Tischstakes verlangt Virtualisierung für lange Diffs/Listen; kein Virtualizer installiert, minimaler deterministischer Eigenbau statt neuer Abhängigkeit. |
| Transkript-Datenform (R2) | **`TranscriptBlock`** (`message` \| `tool` \| `permission`), geordnete Liste je Session via `AgentProvider.getBlocks()` | angewendet | Der Prototyp-Transcript verschachtelt Messages/ToolActions/PermissionPrompts in fester Reihenfolge; die R0-`Message[]`/`ToolAction[]`-Listen tragen keine Reihenfolge. `getBlocks` ist die geordnete Quelle; die alten Flach-Getter bleiben kompatibel. |
| Session-/Chat-Tab-Höhe + Trim (Design-Sync P2) | **`SessionTab` fix `h-9` (=36px)**; Titel `max-w-40` (160px) `overflow-hidden text-ellipsis whitespace-nowrap` (`.st-title`); Button `min-w-0` damit Flex-Ellipse greift | angewendet | `h-[var(--tabbar-h)]` als Arbitrary-Class auf dem Leaf löste in diesem Build **nicht** zu einer Höhe auf (Container blieb content-hoch 17px); die statische Utility `h-9` ist 36px und immer generiert. Lange Chat-/Agent-Titel trimmen mit Ellipse; **Datei-Tabs bleiben ungetrimmt** (P1). Computed-Style-Assert (36px + `text-overflow: ellipsis`). **Agents- + Shell-/Tooling-Goldens neu generiert** (Tab füllt jetzt volle 36px): `agents-dark/light`, `terminal-open-dark`, `services-collapsed-dark`, `data-prod-dark`, `alerts-overlay-dark`, `alerts-pinned-dark`. |
| Transkript-Virtualisierung (R2) | **`VirtualTranscript`** (variable-height windowing via ResizeObserver, keine neue Dep) | angewendet | Chat-Blöcke haben variable Höhe (Message vs. ToolAction vs. PermissionPrompt); `VirtualList` (fixed-row) passt nicht. Misst Höhen, cached sie im State, hält eine Offset-Tabelle. Deterministisch (Estimate → gemessen). 500-Block-Session `s4` als Gate. |
| Chat = parametrisierte Agents-Komponente (Design-Sync P3) | **`AgentWorkspace({kind})`** + `ChatWorkspace = <AgentWorkspace kind="chat" />`; Store-Factory `createAgentsStore(opts)` → zwei Instanzen `useAgents` / `useChat` (`capisco-chat`-Key, Default-Modell Sonnet); `storeForKind(kind)` wählt; eigener `chatSnapshot` (Message-only Blocks `c1`/`c2`, keine Tool/Permission); Chat-Modus `"chat"` in `WorkspaceMode`, `message-square`-Button in der rechten Bar, `mode==="chat"`→`ChatWorkspace` | angewendet | Roadmap P3 „eine parametrisierte Komponente". Agents + Chat sind exakt dieselbe UI (Tabs/Modell-Picker/Composer/Settings/Budget-Meter) über zwei Store-Instanzen; der einzige Unterschied (Subagents + Tool-Actions) lebt in den gerenderten Daten, nicht im Store. Chat: kein Subagent-Row, `useLivePermission("")` inert, Status „quick chat · no tools". Eigener Store statt geteiltem, damit Chat-Sessions/aktive-ID unabhängig sind. **Neuer Golden `chat-dark`**; bestehende Goldens unverändert (rechte Bar wächst nach unten, kein Shift im Viewport). |
| Context-Budget-Meter (Design-Sync P4, reine Projektion) | **`ContextBudgetMeter`** links in der `ComposerBar`; `used = contextUsed(session)` (= `tokensIn+tokensOut` der aggregierten Session-Telemetrie, **kein** neues Datenmodell), `budget` als ephemerer Store-Wert (Default 200k, `setBudget`); Ton `budgetTone(used,budget)` grün<60% · orange<85% · sonst rot; Schwellwert-Popover (Slider 50k–400k + Presets 100k/150k/200k/300k); Rot-Banner über dem Eingabefeld bei `crit`, **[New session]/[Keep going] = No-op-Stubs** (nur Banner-Dismiss, keine Session-Mutation) | angewendet | Council-Grenze P1/AK-D1: das Meter ist **reine visuelle Projektion** (Pixel + Klassen-Flip), die Mechanik (Rot→Handoff/RTK/Caveman) lebt in `road-to-token-economy` P2. `data-tone`-Klassen-Assert statt des (volatilen) Zahl-Strings; Banner bei `used ≥ redAt`. Meter erscheint in **Agents + Chat** (geteilte ComposerBar). Pixel-Delta unter `maxDiffPixelRatio 0.01` → bestehende Goldens trippten nicht; **`agents-dark/light` + `chat-dark` dennoch neu generiert**, um die Meter-Fläche visuell zu pinnen. **Sicht-Abnahme (Klasse C) durch Matze offen** — Implementierung + DOM + Goldens fertig, finale Fidelity-Abnahme ist menschlich. |
| Reduced-motion (global, R2) | **`@media (prefers-reduced-motion: reduce)`** in `globals.css` kollabiert alle Transitions/Animations | angewendet | Tischstakes §5; settled zugleich die Mount-Farb-Transition (Broker-Button-Anomalie, s.u.). |

## Offene Entscheidung — a11y color-contrast vs. JetBrains-Dark-Palette

Die Visual-Harness (axe) meldet `color-contrast`-Verstöße: die freigegebene Palette nutzt
bewusst **gedämpften Sekundärtext** (`--muted-foreground` `#868781`), der auf dunklem Grund
~3.82:1 erreicht — WCAG AA verlangt 4.5:1 für 12px-Text. Das ist eine reale Spannung
zwischen Design-Fidelity und a11y.

**Aktuell:** `color-contrast` ist **getrackt, nicht gegated** (im Visual-Test berichtet, alle
anderen serious/critical-Regeln bleiben harter Gate). Zu entscheiden in `road-to-design-primitives`:

1. **Token leicht anheben** (z.B. `--muted-foreground` → ~`#9a9b96`) → AA erfüllt, Look bleibt „dezent". (empfohlen)
2. **Muted-Look behalten**, color-contrast als dokumentierte, bewusste Abweichung (wie viele Pro-IDEs).
3. **Strikt AA überall** — größter Eingriff in die Ästhetik.

→ Offen für Produkt-Owner-Entscheidung. **Default angewendet:** `--muted-foreground`
`#868781` → `#9d9f96` (dark), erfüllt AA, „dezent"-Look bleibt. Reduziert die Violations
auf dem Foundation-Landing von 5 → 3.

**Anomalie GELÖST (R2, realer Broker-Prompt).** Diagnose im echten Agents-Modus: der
`bg-primary`-Button (PermissionPrompt-`Allow once`) zeigt direkt nach dem Mount kurz einen
falschen Teal- + hellen Textton, weil `transition-colors` (von `tailwindcss-animate`) die
Hintergrund-/Textfarbe vom Default-(Light-)Wert zum `.dark`-Wert **interpoliert** — ein
`getComputedStyle`-Read mitten in dieser Transition liefert den Zwischenwert
(rgb(46,149,137) statt rgb(62,182,168)). Die CSS-Kaskade ist korrekt: `--primary-foreground`
= `#0E1413` (dark) gewinnt; ein frisch eingefügtes Kind im PermissionPrompt löst
`hsl(var(--primary))` korrekt zu rgb(62,182,168) auf. Nach `transition:none` bzw. nach dem
Settling misst der Button rgb(62,182,168) auf rgb(15,20,19)-Text → guter Kontrast.

**Fix:** globale `@media (prefers-reduced-motion: reduce)`-Regel in `globals.css` kollabiert
Transitions/Animations (Tischstakes §5 — `prefers-reduced-motion` respektieren) und settled
damit auch die Theme-Farb-Transition sofort. Der Visual-Test
(`test/visual/agents.spec.ts`) verifiziert den **settled** Button-Zustand: dark
`primary-foreground`-Text auf Teal, Kontrast > 4.5:1. Kein Token-Bug — Transitions-Artefakt.

## Visual-Golden — Plattform-Notiz

Pixel-Goldens (`test/visual/**-snapshots/*.png`) sind **OS-spezifisch** (committed: darwin).
CI (Linux) fährt die OS-unabhängigen Gates (DOM/testid + axe); der Pixel-Golden ist lokaler
Dev-Gate. Ein Linux-Baseline (im Playwright-Container generiert) ist ein Follow-up.

## Test-Umgebungs-Notiz

Node 25 bringt ein experimentelles globales `localStorage` mit, das ohne
`--localstorage-file` nicht funktioniert und jsdoms überschattet. `src/test/setup.ts`
stubt deshalb einen deterministischen In-Memory-Storage.

## Backend-Contracts v2 (B-pre)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Contract-Async-Form | **Alle Provider-Methoden `Promise<…>`** (AgentProvider, EditorProvider, SignalProvider, neue GitProvider/TasksProvider/WorkspaceProvider) | angewendet (B-pre) | Echtes ACP/Git/IPC ist async+streaming; die Interfaces sind der Real-Adapter-Seam. |
| Async-Consumer-Strategie | **Streaming-Fläche (Sessions) nutzt `subscribe`; SNAPSHOT-Views (Editor/Git/Tasks/Signals/Data) lesen synchrone `*Snapshot`-Facaden** | angewendet (B-pre) | Roadmap-Akzeptanz: „kein UI-Consumer pollt mehr, **wo Events passen**". Dashboards sind Snapshot-Views, keine Event-Streams — eine synchrone Facade (gleiche Methodennamen, deterministische Daten) vermeidet Loading-State-Churn ohne Verhaltensänderung; der async Provider bleibt der Contract-Seam (durch Tests + die Session-Streaming-Pfad exerziert). Kein `useAsync`-Hook nötig (verworfen, da kein Consumer ihn brauchte). |
| Session-Event-Kanal | **`subscribe(sessionId, listener) → Unsubscribe`** + `SessionEvent`-Union (`token`/`status`/`tool`/`permission`/`telemetry`/`done`) | angewendet (B-pre) | ACP-shaped. Mock replayt eine deterministische Sequenz auf einem Microtask (kein Polling, kein `Date.now`). |
| Permission-Rückkanal | **`resolvePermission(decision) → GrantAxis`** + `GrantAxis = once\|session\|scoped\|deny` | angewendet (B-pre) | §3 Rückkanal. **Kein „forever/always"-Wert im Typ** → ein Dauer-Grant ist strukturell unkonstruierbar. `once` wird nicht persistiert (single-shot); alles andere im Broker-Grant-Store gemerkt. `PermissionRequest.fromUntrusted` markiert lethal-trifecta-Gates (§3.3). |
| Telemetrie | **`Telemetry {tokensIn,tokensOut,runtimeMs}`** statt `meta`-String; `aggregateTelemetry` rollt Subagent→Parent (Tokens summiert, Runtime = Wall-Clock-Max); UI rendert via `formatTelemetry` (in Goldens maskiert) | angewendet (B-pre) | Roadmap Phase 1. `s1`-Tab zeigt aggregiert 7.7k (6.5k Parent + 1.2k Subagent). |
| Repo ≠ Worktree | **`Project` → `Repo` (remote/defaultBranch) + `Worktree` (repoId/path/branch/base)**; `Project = Worktree`-Alias (deprecated); `mockProjects = mockWorktrees`-Alias | angewendet (B-pre) | §2.1: ein Repo, N Worktrees. Explorer/Changes mappen auf Worktree; Alias hält Straggler-Consumer grün ohne Big-Bang-Rewrite. |
| Datasource-Readonly | **Derived non-settable Invariante** via `makeDatasource(input)`: `readonly = env==="production"`, `Object.freeze`, Feld TS-`readonly`; `WriteEscape`/`makeWriteEscape` = per-Befehl-einmal (`consumed`-Flag, **kein** Session-/Scope-Feld) | angewendet (B-pre) | §3.3 Security-Fix: der optionale `readonly?`-Toggle (Smell) ist weg. „Permanent prod-write erlauben" ist strukturell unkonstruierbar (kein Konstruktor-Pfad, gefrorenes Objekt, kein settbares Feld). |
| Session-Tree | **`SessionTree`/`SessionNode` (parentId/children/branchLabel)** + `getTree`/`branch(nodeId,label)` | angewendet (B-pre) | §2.2 retry-as-branch: `branch()` graftet ein Geschwister, **überschreibt den Parent nie**. Mock baut einen linearen Baum aus den Blocks; Branches persistieren im In-Memory-`TREES`. |
| History-2 | **`ShadowStore`-Interface** (`record/list/get/restore`, `SnapshotReason = save\|external`) + `createInMemoryShadowStore` (deterministischer `seq`, append-only) | angewendet (B-pre) | §5.1 „Agent hat meine Datei zerlegt"-Rettungsnetz, getrennt von Git. Echter content-addressed Store ist ein dünner Swap hinter dem Contract. |

## Tauri-Sidecar (B0)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Sidecar-Ort | **`app/sidecar/`** (Node-only TS) + browser-sichere Protokoll-/Client-Hälfte unter **`src/lib/sidecar/`** | angewendet (B0) | Eine pnpm-Paket-Wurzel, kein neues Install nötig (nur Node-Builtins + vorhandenes TS/Vitest). Aufteilung vermeidet das Cross-Projekt-Double-Compile: die DOM-freie Server-Hälfte (`net`/`fs`) lebt im Sidecar-Projekt, die transport-agnostische Protokoll-/Client-Hälfte im App-Projekt; der Browser-Build importiert nie `node:net`. |
| IPC-Transport | **JSON-RPC 2.0 über NDJSON** (`\n`-getrennte Zeilen) über **Unix-Socket**; Events als id-lose `$/event`-Notifications, gemultiplext per Channel `<provider>:<targetId>` | angewendet (B0) | §4.1/§6: NDJSON-stdio-Sidecar-Muster (ACP-shaped). `NdjsonDecoder` toleriert beliebige Chunk-Grenzen (Framing/Backpressure-Tests: 1-Byte-Flut, gepackte Zeilen, Teilzeilen, Byte-Cap gegen Runaway-Peer). |
| Transport-Seam | **`Transport`-Interface** (send/onData/onClose/close) + `createPipePair()` (In-Process) | angewendet (B0) | Dieselbe Peer-Logik (Request-Korrelation, Event-Dispatch) läuft über echten Socket UND In-Process-Pipe → der TS-IPC-Harness und die Tests exerzieren exakt die Socket-Codepfade, ohne ein OS-Socket zu brauchen. |
| Provider-Registry | **`ProviderRegistry`** mappt In-Process-Provider auf flache `"<id>.<method>"`-RPC-Fläche; `params` = positionales Arg-Array | angewendet (B0) | §2.3 „der Stecker": der Kern ist dünn (Registry + IPC-Server). Streaming (`subscribe`/`unsubscribe`) ist out-of-band im IPC-Server, die Registry bleibt reiner Methoden-Router. |
| Mock→Real Swap-Punkt | **`DesktopShell`-Seam** (`src/lib/desktop-shell.ts`): Bridge vorhanden (`globalThis.__CAPISCO_SIDECAR__`) → IPC-Proxies über `SidecarClient`; sonst → In-Process-Mocks | angewendet (B0) | Akzeptanz: „echter Sidecar-Client, Browser-Stub bleibt Fallback". Kein UI-Consumer weiß, welche Seite er anspricht — beide erfüllen dieselben `contracts/`. Browser-sicher: die Bridge liefert ein fertiges `Transport`-Objekt, der Desktop-Host besitzt den echten Socket. |
| Pure-Lookup-Parität | IPC-Proxy für git/tasks borgt die **synchronen** Contract-Lookups (`labelChartVar`/`epicLabel`/`typeChartVar`, `overdueThresholdDays`/`wipLimit`) **verbatim vom Mock** statt sie zu round-trippen oder zu duplizieren | angewendet (B0) | Die Lookups sind deterministische Konstanten und im Contract synchron. Verbatim-Borgen vermeidet Drift; ein Paritäts-Test pinnt es. |
| Rust/Tauri-Shell | **Dünner dokumentierter Stub** (`sidecar/shell-stub/README.md`), **deferred** (kein `cargo`); verifizierbarer Zwilling = **TS-IPC-Harness** (`sidecar/harness/`) | angewendet (B0) | §5 Deferred: CI hängt nicht an einem Tauri-Build. Der Harness stellt denselben Sidecar + dieselbe Registry hinter demselben JSON-RPC über eine In-Process-Pipe auf → der echte Rust-Bridge ist ein reiner Transport-Swap. |
| Cross-IDE Recent-Projects | **Passive maschinenweite Datei** (`~/.config/capisco/recent-projects.json`, override `CAPISCO_RECENT_FILE`); `createFileRecentProjects` mit **atomic write** (temp+rename) + **Read-Merge-Write per `path`**; In-Memory-Mock als Browser-Fallback | angewendet (B0) | §6: kein Daemon/Socket-Mesh/LSP-multi-root. Nebenläufigkeit über atomic rename (POSIX-atomar → kein Half-Read) + Merge-by-path (Update-in-place, fremde Instanzen bleiben erhalten). `lastSeen` = opaker Logical-Clock-Ordinal (nicht Wall-Clock) → deterministisch testbar. Korruptes File heilt beim nächsten Write. |
| Projekt-Switcher | **`ProjectSwitcher`** im TitleBar (ersetzt den Projekt-`ChromeButton`), liest `recent.list()` über den Provider-Bundle, surfaced andere Instanzen/Projekte (Branch + Active-Dot) | angewendet (B0) | §6 / Phase 2: Referenz/Jump ohne beide Projekte ins selbe Fenster zu laden (bewusster Nicht-Ziel; Cross-Projekt-Session-Suche = broker-scoped B3-Follow-up). Pixel-Golden unverändert (gleiches „capisco"-Label, gleiches Layout). |

## Real-Git (B1)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Git-Lib-Wahl | **Shell-out zum System-`git`** (nicht isomorphic-git, nicht libgit2-Binding) via `node:child_process.execFile` ohne Shell | angewendet (B1) | System-`git` (2.39.5) ist vorhanden → null neue Dependency (kein Lockfile-Churn, minimal-safe-diff), höchste Fidelity (echte Porcelain-Ausgabe), hermetisch gegen `git init` Temp-Repos testbar. isomorphic-git wäre eine neue Dep + eigene Bug-Fläche; libgit2 braucht ein natives Binding (kein Toolchain-freier Swap). Roadmap-Phase-0 nennt „shell-out/libgit2" explizit als Option. |
| Git-Exec-Sicherheit | **`execFile("git", ["-C", cwd, ...argv])` — nie über eine Shell**; nur `git`-Subkommandos fließen durch; 10-MiB-Buffer-Cap; `GitError` mit Code+stderr | angewendet (B1) | Kein Shell-Injection-Surface (Pfade/Refs/Messages sind diskrete argv-Argumente, nicht interpoliert). Das ist das First-Party-Execution-Primitive, das der Broker (B4) später mediiert — kein offener Shell-Escape. |
| Primitive vs. UI-Contract | **Zwei Contracts:** `GitOpsProvider` (`contracts/git-ops.ts`, primitiv: status/diff/log/blame/branches + stage/unstage/commit/branchCreate/checkout, jede Methode per `cwd` gekeyt) **+** `RealWorkspaceProvider` projiziert in die bestehenden UI-Shapes (`DiffDoc`/`ChangeSet`/`WorkStash`/`Worktree`) | angewendet (B1) | Der bestehende `GitProvider` ist die GitHub-style PR/DORA-**Dashboard**-Fläche (braucht eine Forge → bleibt Mock bis B6); der lokale Git-Porcelain ist eine eigene, git-treue (porcelain-v2/numstat/`--porcelain` blame) Achse. Trennung hält die Shapes klein und parser-testbar; die UI-Projektion ist eine dünne Schicht obendrauf. **Keine** UI-Contract-Änderung → alle UI-Gates trivial grün. |
| Diff-Berechnung | **Zwei Pässe** (`diff -M` Patch für Hunks + `diff --numstat -M` für Counts/Binary/Rename), Working/Staged (`--cached`)/Branch-vs-Base (`<ref>`) | angewendet (B1) | numstat liefert add/removed + Rename-Erkennung robust; der Patch liefert die Hunk-Zeilen. Reine Parser in `git-parse.ts` (unit-getestet auf Fixture-Strings, kein Prozess-Spawn). |
| Commit-Identity (hermetisch) | **`-c user.name=…` / `-c user.email=…`** prozess-lokal (statt `process.env`-Mutation) wenn ein `GitAuthor` übergeben wird | angewendet (B1) | env-Mutation ist unter Nebenläufigkeit unsicher; `-c`-Flags sind prozess-lokal und deterministisch. Temp-Repos pinnen `Test Bot <bot@capisco.test>` → maschinen-unabhängige Hashes/Autoren. |
| Registrierung / Swap | **`registerGitProviders(registry, {cwd})`** (`sidecar/register-git.ts`) registriert `workspace` (real) + `gitops` unter stabilen IDs; Default-Boot bleibt Mock (deterministische UI-Parität), real ist der dünne Swap | angewendet (B1) | „real wo verifizierbar, fake wo extern" — der reale Git-Adapter ist ein reiner Registry-Swap hinter derselben Wire-Fläche; über den echten IPC-Spine (Socket → Registry → RealGitProvider) end-to-end verifiziert (`real-git-ipc.test.ts`). |
| Out-of-B1-Scope | **`getSearch`/`getStructure`/`listScratches` = ehrliche Empties** im RealWorkspaceProvider; `WorkStash.shelf` = leer (real `git stash list` ist Follow-up) | angewendet (B1) | ripgrep-Search + LSP-Symbol-Outline + Stash-Shelf brauchen Tooling außerhalb von B1; ehrliche Empties statt fingierter Daten (nie ein Real-Integration fälschen). |

## Worktree-Runtime (B2)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Worktree-Primitive vs. UI-Contract | **Eigener `WorktreeOpsProvider`** (`contracts/worktree.ts`: create/list/remove/gc, jede Methode per `repoCwd` gekeyt) getrennt von der UI-facing `WorkspaceProvider.listWorktrees`-Projektion (B1) | angewendet (B2) | §2.1 „ein Repo, N Worktrees": das Lifecycle-Primitive (anlegen/zerstören/GC eines Agent-Laufs) ist eine andere Achse als die Explorer/Changes-Projektion eines *einzelnen* Worktrees. Trennung hält die Shapes klein + git-treu (porcelain-`worktree list`), parser-testbar. Keine UI-Contract-Änderung. |
| Git-Worktree-Impl | **Shell-out via dieselbe `git()`-Helper wie B1** (`git worktree add/list --porcelain/remove/prune`), `RealWorktreeProvider` (`sidecar/git/real-worktree-provider.ts`) | angewendet (B2) | Konsistent mit B1 (System-`git`, kein Shell, null neue Dep, hermetisch gegen `git init` Temp-Repos). `git worktree` ist genau die git-native Form des „Ort"-Primitivs — kein Reimplementieren von Checkout-Isolation. |
| Session↔Worktree-Kopplung | **In-Process-Map `path→sessionId`** im Provider (git kennt keine Capisco-Session); `create({sessionId})` koppelt, `remove` entkoppelt, `gc` released Sessions deren Worktree-Dir verschwand | angewendet (B2) | §2.1 „Worktree-Lifecycle == Session-Lifecycle". git speichert keine Session-ID → der Provider trägt die Kopplung. GC-bei-Crash: ein Worktree dessen Verzeichnis weg ist (git `prunable`) gibt seine Session via `releasedSessions` frei → der Session-Store (B3) kann den verwaisten Lauf aufräumen. |
| Pfad-Kanonisierung | **`realpathSync` als Coupling-Key** (Fallback `resolve` für nicht-mehr-existierende Pfade) | angewendet (B2) | git meldet symlink-aufgelöste Pfade (macOS `/var`→`/private/var`); ohne Kanonisierung matcht der angeforderte Pfad nicht den gelisteten → `create` fände den eben angelegten Worktree nicht. Fallback `resolve` für gecrashte (gelöschte) Worktrees in GC. |
| GC = `git worktree prune` | **`gc()` misst prunable-Entries VOR dem Prune** (Wahrheit über das, was bereinigt wurde), released gekoppelte Sessions, dann `git worktree prune`; idempotent | angewendet (B2) | git's `prune` ist die native Crash-GC (verwaiste Admin-Entries unter `.git/worktrees`). Vorher-Messung statt `--verbose`-Parsing macht das Ergebnis deterministisch + testbar; idempotent (zweiter Lauf = no-op). |
| RuntimeProvider Interface+Fake | **`RuntimeProvider`-Interface** (`contracts/runtime.ts`: `listServices`/`subscribeStats`/`ports`) + **deterministische `FakeRuntimeProvider`** (`src/mocks/runtime.ts`, scripted `docker stats`-Frames, microtask-replay, kein Date.now/Math.random/Timer) | angewendet (B2) | §7 Container-Runtime ist deferred (kein Docker-Daemon). Interface + deterministischer Fake hinter derselben `contracts/`-Form; echter Docker/Podman-Adapter ist der dünne Swap. `subscribeStats` repliert eine endliche Frame-Sequenz (6 initial + 3 drift) → bewegliche Stats deterministisch verifizierbar. |
| Services-View-Quelle | **Snapshot über `RuntimeProvider.listServices()`** (ersetzt den hartkodierten `mockContainerGroups`-Import); `subscribeStats` ist eine Provider-Capability die der Panel **nicht** auto-applied | angewendet (B2) | Akzeptanz „Services-View läuft gegen den Provider". `listServices` = ein deterministischer `docker ps`+`stats`-Tick (web@34% cpu, 3/4 up) → der Visual-Golden (`tooling-breadth.spec.ts`) bleibt stabil. Live-Drift-Frames würden den Golden brechen → Stream bleibt eine vom Fake selbst getestete Capability, nicht im gerenderten Snapshot. |
| Port-Allocator-Stub | **`StubPortAllocator`** (deterministischer Range 49152–49251, in-order Vergabe, Owner-Tracking, idempotentes Release, wrap auf freigegebene Ports) hinter `RuntimeProvider.ports()` | angewendet (B2) | Roadmap „Port-Allocator-Stub". Kein OS-Binding (deferred) — der reale Adapter probt echte Verfügbarkeit/Traefik; bis dahin ist dies das Reservierungs-Seam (eine Worktree-Service-Gruppe bindet kollisionsfrei). |
| Registrierung | **`worktree-ops`** in `registerGitProviders` (real, git-backed), **`runtime`** in `registerMockProviders` (Fake); nur `listServices` ist RPC-bar (`subscribeStats` out-of-band, `ports()` lokaler Allocator) | angewendet (B2) | Worktree-Primitive über den echten IPC-Spine verifiziert (`worktree-ipc.test.ts`: create/list/remove über Socket). Der Fake-Runtime ist im In-Process-Registry für den Harness erreichbar; die nicht-serialisierbaren Capabilities (Stream, Allocator) leben im In-Process-Consumer. |
| Container-Runtime deferred | **Echter Docker/Podman + Traefik-Routing = eigene `later/`-Roadmap** (Interface+Fake gebaut, echter Swap braucht Docker-Daemon) | angewendet (B2) | Konzept §8 value-first: Container nicht vor den agent-nativen Kern ziehen. Der Fake erfüllt heute den Contract; der reale Adapter ist ein dünner, dokumentierter Swap sobald ein Daemon da ist. |

## Capability-Broker (B4)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Broker-Contract-Ort | **`contracts/broker.ts`** (`Principal`/`CapabilityRequest`/`BrokerDecision`/`PolicyEngine`/`SecretStore`/`AuditStore`/`CapabilityBroker`/`ExecutionContext`/`GrantConfig`/`AllowlistRule`) | angewendet (B4) | Die Security-Invarianten werden in die **Typ-Form** kodiert (gefährliche Zustände strukturell unkonstruierbar), nicht nur zur Laufzeit verteidigt. Re-exportiert via `contracts/index.ts`. |
| Chokepoint-Impl | **`Broker` (`sidecar/broker/capability-broker.ts`)** komponiert PolicyEngine + SecretStore + AuditStore; `authorize` (pure, auditiert) → `execute` (re-decide, wirft bei ≠allow) | angewendet (B4) | MUST-NOT 1: kein Execution-Edge ohne `allow`. `execute` re-autorisiert am Ausführungspunkt — der einzige Pfad zu Shell/File/Netz/DB/Secret. |
| Policy-Engine | **`GrantPolicyEngine` (`sidecar/broker/policy-engine.ts`)**: fail-closed (`decide` ohne Allow-Regel → `ask`), per-Projekt+Scope Grant-Store, `resolve()` = §3-Rückkanal | angewendet (B4) | `(Principal × Capability × Scope) → decision`. Mensch ist kein Bypass (gleicher Gate). `once` nicht persistiert; `session`/`scoped`/`deny` per Projekt; **kein** Forever-Wert (GrantAxis hat keinen). |
| Default-Allowlist | **`DEFAULT_GRANT_CONFIG` (`sidecar/broker/default-grants.ts`)** — Config, nicht Code | angewendet (B4) | §4 human-gated: konservativ (read-only allow, mutating ask, `rm`/`sudo` deny). Build erfindet keine weite Allowlist. PolicyEngine liest die Config, hartcodiert keine Verdikte. |
| Secret-Tresor | **`SecretStore`-Interface + `InMemorySecretStore`** (`sidecar/broker/in-memory-secret-store.ts`); **kein `get(ref): string`** — nur `inject(ref, use)` scoped den Wert in einen Callback | angewendet (B4) | MUST-NOT 2: capability-by-reference. `list()` = nur Referenznamen. Wert verlässt den Store nie als Wert/env/CLI-arg. Execution-Layer-Injection via `ExecutionContext.withSecret`. **Deferred:** echter OS-Keychain als dünner Swap. |
| Audit-Store | **`InMemoryAuditStore`** (`sidecar/broker/audit-store.ts`) — append-only (nur `record`/`list`, gefrorene Snapshots, monotone `seq`); refuse value-shaped `credentialRef` | angewendet (B4) | MUST-NOT 5: vor Ausführung geschrieben (Akteur + Capability + credentialRef, nie Wert). Kein update/delete → Tampering strukturell unmöglich. |
| Prod-Write-Invariante | **`Broker.execute` erzwingt** für prod `db-write` einen frischen, unverbrauchten, command-gematchten **`WriteEscape`** (aus B-pre: `{datasource,command,consumed}`, **kein** session/remember-Feld); danach `consumed=true` (auto-revert) | angewendet (B4) | MUST-NOT 3: „dauerhaft prod-write erlauben" ist strukturell unkonstruierbar (kein session-weites Escape-Shape). `productionDatasources` ist **human-confirmed** Config, nie aus Connection-String inferiert (Default leer). |
| Egress-Gate | **PolicyEngine forciert** `fromUntrusted` + Egress-Kind (`file-write`/`network`/`db-write`/`external-write`) → `ask`; ein `session`/`scoped`-Grant kann es **nicht** vorab freigeben | angewendet (B4) | MUST-NOT 4 (Lethal-Trifecta): untrusted Agent/Ticket/Web-Output = Daten, nie Instruktionen. Jeder Egress daraus = harter Human-Gate, nie auto. |
| Bidirektionaler Tresor-Write | **`proposeVaultWrite`/`commitVaultWrite`** — Proposal auditiert, kein Wert gespeichert; `commit` wirft ohne `approved` | angewendet (B4) | §3.2: Agent legt Test-User an → Vorschlag in den Tresor, mit Freigabe, nie in den Chat. |
| RPC-Fläche | **`BrokerProvider` (`sidecar/broker/broker-provider.ts`)** registriert als `broker`; nur `authorize`/`resolve`/`listSecretRefs`/`listAudit` über die Wire | angewendet (B4) | `execute` + Secret-VALUE-Pfad sind **nicht** auf der Wire (Wert würde in Log/Session-Store landen) — leben in-process am Execution-Layer (spiegelt B2-Runtime-Split). Über echten Socket-Spine verifiziert (`broker-ipc.test.ts`). |
| Wiring / Default-Boot | **`registerBroker`** in `createSidecar` (main.ts) + TS-IPC-Harness; Default = leere Prod-Datasource-Liste | angewendet (B4) | B4-Gate **vor** B3 (ACP wired dahinter) — der Stub-Agent kann nicht um den Broker herum handeln. |

## Session-Store & ACP (B3)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Session-Store-Contract | **`contracts/session-store.ts`** (`StoredSession`/`ResumedSession`/`SessionSearchHit`/`SessionStore`) — `create/list/get/append/update/resume/retryAsBranch/copy/search`, alle `Promise<…>` | angewendet (B3) | §2.2 „Store ab Tag eins"-Pflicht. Resume/Suche/Retry-as-branch/Copy fallen aus einem Datenmodell (Record + Block-Log + `SessionTree` + invertierter Index). Async = Real-Disk-Store-Seam. |
| Session-Store-Impl | **`InMemorySessionStore` (`sidecar/session/in-memory-session-store.ts`)** — deterministisch (monotoner `seq`, kein Date.now/Math.random); `retryAsBranch` graftet ein Geschwister, **überschreibt Parent nie**; `resume`/`copy` deep-clonen (Reads aliasen den Store nie) | angewendet (B3) | §2.2 retry verzweigt; Reads sind Klone (Tamper-Test). **Deferred:** echter SQLite/content-addressed Store = dünner Swap hinter dem Contract. Methoden `async` → synchroner Throw wird zur Rejection (Resume-unknown-Test). |
| ACP-Contract | **`contracts/acp.ts`** (`AcpToolCall`/`AcpPermissionRequest`/`AcpPermissionResponse`/`AcpSessionUpdate`/`ACP_METHODS`) | angewendet (B3) | §4.1 ACP als Transport: JSON-RPC/stdio. `session/new`+`session/prompt` (client→agent), `session/request_permission` (agent→client = Broker-Seam), `session/update` (id-lose Notification) → mappt 1:1 auf `SessionEvent`. |
| Stub-ACP-Agent | **`sidecar/acp/stub-acp-agent.mjs`** — deterministischer Child-Prozess (pures Node, kein TS-Import), scripted stdio | angewendet (B3) | §5 Deferred: echte CLIs (Claude Code/Codex/Gemini) + LLM-Keys = dünner Swap über dieselben stdio-Frames. **Harte Eigenschaft:** der Stub hat **keine** direkte fs/shell/net-Capability — sein einziger Aktions-Pfad ist `session/request_permission` + Warten auf die Client-Antwort → kann nicht am Broker vorbei handeln. |
| ACP-Transport | **`sidecar/acp/acp-transport.ts`** — JSON-RPC/NDJSON-Peer über Child-`stdio`, reused `NdjsonDecoder` (beliebige Chunk-Grenzen), korreliert Request-IDs, dispatcht agent→client-Requests an `onAgentRequest` | angewendet (B3) | Framing-treu zum Socket-Transport (B0). Spawnt `node stub-acp-agent.mjs`; `STUB_ACP_AGENT_PATH` exportiert. |
| Broker-Gate (Invariante) | **`AcpSession` (`sidecar/acp/acp-session.ts`)** beantwortet **jede** `session/request_permission` **ausschließlich** über `broker.authorize` → Human-Gate → `broker.execute`. Kein Code-Pfad führt eine agent-angeforderte Aktion ohne den Broker aus | angewendet (B3) | Akzeptanz: „Keine Agent-Capability ohne Broker-Call" (Architektur-Test). `deny`/`ask`-deny blockiert die Aktion; `executed`-Audit vor dem Side-Effect. |
| Untrusted-Egress-Laundering | **Agent-`authorize` behält `fromUntrusted` (→ harter `ask`, nie auto, MUST-NOT 4).** Erst eine **explizite Per-Call-Human-Entscheidung** führt die Ausführung als **`human`-Principal** über die **trusted** Request-Form aus (§3.1: Mensch ist der verantwortliche Principal, §3.3-Laundering) | angewendet (B3) | Der Broker (`decide`) lässt untrusted Egress **nie** zu `allow` → `execute` würde werfen. Der Agent clearet seine eigene untrusted Egress nie; nur ein Mensch tut es, per Call. Audit hält den untrusted-Gate-Record + den `executed`-Record (als human). **`once` kann der Broker nicht ausführen** (re-decide kennt es nicht) — `session` ist die natürliche „Ja für diesen Lauf"-Achse; dokumentierte B4-Grenze, keine Aufweichung. |
| ToDo-Contract + Parser | **`contracts/todo.ts`** (`TodoItem`/`TodoView`/`TodoStatus`/`TodoProvider`/`TodoParser`) + **`src/lib/todo/todo-parser.ts`** (pure, browser-safe; `- [ ]`/`* [x]`/`1. [ ]`) | angewendet (B3) | §4.11 Mikro-Nordstern. Parser ist DOM-frei (Markdown-Editor ruft ihn auf dem Buffer). Erkennt eingerückte + ordered/bullet-Marker, Zeilennummer + checked-State. |
| ToDo-Provider | **`sidecar/todo/todo-provider.ts`** + **`acp-todo-starter.ts`** (`TodoSessionStarter`-Seam → broker-gegatete `AcpSession` im aktuellen Worktree) | angewendet (B3) | §4.11: ToDo open → in-progress (sofort) → done (Session-Status). Kein neues Primitiv (§9 Konvergenz). ToDo-Text ist **untrusted** (Datei/Web-authored) → der vom Agent abgeleitete Egress trifft den Trifecta-Gate. Starter-Throw revertet auf `open` (nie still „done"). |
| RPC-Fläche | **`registerSession` (`sidecar/register-session.ts`)** registriert `session` (Store) + `todo` über die Wire; ACP-Child-stdio + Secret-Execution-Layer **nicht** auf der Wire | angewendet (B3) | Spiegelt B2/B4-Split: nur JSON-shaped, secret-freie Fläche serialisiert. Über echten Socket-Spine verifiziert (`session-ipc.test.ts`). Default-Human-Gate fail-closed (deny-all); reale UI swappt einen Prompt-Resolver. |
| Wiring / Default-Boot | **`registerSession`** in `createSidecar` (main.ts) + TS-IPC-Harness, **hinter** `registerBroker` (Broker-Instanz übergeben) | angewendet (B3) | Jede agent-Capability fließt weiter durch den Chokepoint. CI hängt nicht an einem echten Agenten/Key (Stub deterministisch). |
| Cross-Projekt-Session-Suche | **Deferred** (broker-scoped, Secret-Leak-Gate) — Follow-up `later/` | offen (B3) | §3.2 Risiko: fremder Projekt-Context darf nicht in einen Cloud-Prompt lecken. Braucht Dich. |

## Quality-Grounding (B5)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Quality-Contract-Ort | **`contracts/quality.ts`** (`QualityProvider`/`Diagnostic`/`QualityFix`/`QualityRunResult`/`QualityToolId`/`LanguagePack`) + **`contracts/ai-review.ts`** (`AiReviewProvider`/`AiReview`/`ReviewedFinding`) | angewendet (B5) | Tool-agnostische Diagnostic-Form (`tool/file/line/column/severity/rule/message/fix`), die UI / Signal-Fläche / AI-Review alle als **eine** Form konsumieren. Re-exportiert via `contracts/index.ts`. |
| Tool-Runner-Impl | **`RealQualityProvider` (`sidecar/quality/real-quality-provider.ts`)** — `execFile` ohne Shell (gleiche Posture wie `git-exec.ts`) auf die installierten Binaries (`node_modules/.bin/{eslint,tsc,vitest}`, aufgelöst aus dem App-Root, damit ein Fixture-Worktree ohne eigene node_modules läuft) | angewendet (B5) | Akzeptanz „läuft Tool im Worktree, parst Output → Diagnostics + Fixes. Gegen Fixture-Dateien verifiziert (real, kein Fake)". eslint `--format json`, tsc `--noEmit --strict --pretty false`, vitest `run --reporter=json --no-color --root <cwd>`. Exit-Code ist **nicht** die Wahrheit (non-zero = Tool fand Probleme) — der Parser ist es; `run` rejected nie auf non-zero. |
| Pure Parser | **`sidecar/quality/quality-parse.ts`** (`parseEslint`/`parseTsc`/`parseVitest`) — kein Prozess-Spawn, unit-getestet auf Fixture-Strings (Muster wie `git-parse.ts` B1) | angewendet (B5) | Trennung Parser ↔ Runner: die Parser sind deterministisch auf rohen Output-Strings testbar; der Integrationstest exerziert die echten Binaries. Pfade werden via `realpathSync`-Kanonisierung worktree-relativ (macOS `/var`→`/private/var` sonst spurious `../…` — gleiche Lösung wie B2-Worktree-Coupling). |
| ESLint-Fixture-Form | **Plain `.js` + self-contained Flat-Config** (`prefer-const: error`, `no-unused-vars: warn`) im Fixture-Worktree | angewendet (B5) | TS-Type-Annotations brauchen den typescript-eslint-Parser (Config-Last); plain JS mit Default-Parser hält die Fixture hermetisch ohne zusätzliche eslint-Plugin-Config. Eine fixable `prefer-const`-Error + eine `no-unused-vars`-Warning beweisen Diagnostic + anwendbaren Fix. |
| Signal-Fold | **`resultsToSignals` / `QualityProvider.toSignals`** → `SignalItem(source:"lint")` (§5.2): ein Signal je Tool-Result (clean→`success`, nur Warnings→`idle`, Errors→`warning`) | angewendet (B5) | Akzeptanz „Diagnostics in die Signal-Fläche". Pure Fold, standalone unit-getestet; die `lint`-Source existiert bereits im R6-Mock-Rail. |
| Language-Pack-Form | **`LanguagePack`-Interface** + **`typescriptPack`** (eslint/tsc/vitest) als der eine konkrete Pack; phpstan/rector/ecs/ruff/mypy = Interface, **deferred** (je eigener Toolchain) | angewendet (B5) | Akzeptanz „Sprach-Pack-Form als Interface; konkrete Packs deferred". Der TS-Pack IST der reale Pack hinter derselben Form; ein non-TS-Pack ist ein dünner Swap sobald sein Binary da ist. |
| AI-Review-Loop | **`FakeAiReviewProvider` (`sidecar/quality/fake-ai-review-provider.ts`)** — „erst Tools, dann LLM auf Tool-Fakten", LLM-Call **deferred** (Keys) | angewendet (B5) | Akzeptanz „KI-Review-Loop hinter Interface; LLM-Aufruf deferred". Der Fake leitet das Review **rein aus den übergebenen Tool-Fakten** ab (1:1 je Diagnostic, erfindet nie ein Finding) — genau die Disziplin die der echte LLM-Provider wahren muss. Deterministisch (kein Date.now/Math.random/Netz; gleiche Fakten → gleiches Review, getestet). Least-agency: der Provider kann keine Tools laufen lassen / Dateien lesen — er reviewt nur die übergebenen Fakten. Review trägt **keine** ausführbare Aktion (Lethal-Trifecta: LLM-Prosa wäre untrusted, nie Instruktion; Fix = Faktum für human-gated Apply über den Broker). |
| RPC-Fläche / Wiring | **`registerQuality` (`sidecar/register-quality.ts`)** registriert `quality` (Runner) + `ai-review` (Fake) über die Wire; in `createSidecar` (main.ts) **und** TS-IPC-Harness | angewendet (B5) | Runner-Methoden (`run`/`runAll`/`availableTools`/`toSignals`) + Fake-Review sind JSON-shaped und serialisieren über den Socket — über den echten Spine verifiziert (`quality-ipc.test.ts`: echtes eslint über den Socket). Der echte LLM-Provider (deferred) hielte seinen Key im Execution-Layer, nie auf der Wire (spiegelt B2/B4-Split). Keine externe Dependency — Binaries sind first-party. |

## Task-Forge (B6)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Task/Forge-Contract-Ort | **`contracts/task-forge.ts`** (`TaskProvider`/`ForgeProvider`/`TaskFixture`/`ForgeFixture`/`WhoseTurnEntry`/`TaskBackend`/`ForgeBackend`) — **read-only** Provider; **kein** Status-Write auf dem Provider | angewendet (B6) | §4.5/§4.6: Ticketsysteme wie Sprachen — ein Interface, mehrere Backends. Council Lens C: nur **eine** Richtung. Der Write (Status zurück) lebt **nicht** auf dem read-only Provider, sondern im Lifecycle-Orchestrator hinter dem Broker → ein externer Write ist immer eine mediierte, human-gegatete Capability, nie eine stille Provider-Methode. Re-exportiert via `contracts/index.ts`. |
| Provider-Impl + Fixtures | **`FixtureTaskProvider`/`FixtureForgeProvider`** (`sidecar/task-forge/`) nehmen ein Fixture-**Objekt** (kein Pfad) → browser-safe, I/O-frei, unit-testbar; **`load-fixtures.ts`** (node-only `fs`) liest die recorded JSON (`fixtures/{jira,linear}.task.json`, `fixtures/{github,gitlab}.forge.json`) | angewendet (B6) | Akzeptanz „mappt Ticket/PullRequest aus recorded JSON-Fixtures, verifiziert". Trennung Provider↔Loader hält die Provider DOM-frei (gleicher Split wie B0 protocol/server). Defensive Copies — die Provider mutieren die Fixture nie (Test). Deterministisch: kein Date.now/Math.random. |
| „meine Tickets / nächstes aus Sprint / wessen Zug" | **`myTickets`** = `who===me`; **`nextFromSprint`** = unstarted (`todo`>`backlog`), fewer points, id-tiebreak; **`whoseTurn`** = Union aus „ich bin angefragter Reviewer (blockiere das Team)" + „mein PR mit requested changes" (oldest-first) | angewendet (B6) | §4.5/§4.6 Wortlaut. `whoseTurn` behauptet nie, ich reviewe meinen eigenen PR. Beide Backends (jira/linear, github/gitlab) gegen je eigene Identity/Sprint verifiziert (`fixture-task-forge.test.ts`). |
| Live-Tokens deferred | **Echte Jira/Linear/GitHub-API + Webhooks + bidirektionaler Sync = später** (Interface+Fixture gebaut) | angewendet (B6) | §5 Deferred: der reale Adapter swappt den Fixture-Loader gegen einen API-Client; Token wird **im Execution-Layer** injiziert (nie auf der Wire / im LLM-Context / Session-Store / Log — §3.2). Die Wire-Fläche bleibt unverändert. |
| Lifecycle-Contract-Ort | **`contracts/lifecycle.ts`** (`TicketLifecycle`/`LifecyclePhase`/`StatusWriteOutcome`/`TicketStartResult`/`TicketFinishResult`) | angewendet (B6) | §4.5/§7: Ticket→Worktree→Session→Status, **eine** Richtung. `StatusWriteOutcome = written\|gated` kodiert die Invariante in den Typ: ein externer Write ist nur `written` wenn ein Mensch den Broker-`ask` geklärt hat, sonst `gated` — nie still gedroppt, nie auto-gefeuert. |
| Lifecycle-Impl (Untrusted-Egress-Gate) | **`TicketLifecycleImpl` (`sidecar/task-forge/ticket-lifecycle.ts`)** komponiert TaskProvider + RealWorktreeProvider (B2) + SessionStore (B3) + Broker (B4). Ein Ticket ist **untrusted input** → der Status-Write ist `external-write` + `fromUntrusted:true` → harter Broker-`ask`, **nie** auto. Gelaundert wie `AcpSession`: die Automation clearet ihre eigene untrusted Egress nie; nur eine **per-Call-Human-Entscheidung** führt den Write als **`human`-Principal** über die **trusted** Request-Form aus | angewendet (B6) | MUST-NOT 4 (Lethal-Trifecta). Verifiziert (`ticket-lifecycle.test.ts`, gegen Temp-Repo + echten Worktree + echten Store + echten Broker): (a) lokaler Worktree+Session kommen hoch egal wie das Gate ausgeht; (b) Default fail-closed → `gated`, PATCH feuert nie; (c) Broker auditiert den untrusted-`ask` **vor** jeder Execution (kein `executed`-Record); (d) erst ein explizites `session`-Human-Decision → `written`, executed als `human`/trusted; (e) `once` ist nicht persistiert → `execute` re-decided zu `ask` → wirft → `gated` (dokumentierte B4-Grenze). |
| Lifecycle nicht auf der Wire | **`TicketLifecycle` wird in-process vom Consumer konstruiert**, nicht registriert; nur die read-only `task`/`forge`-Provider sind RPC-bar | angewendet (B6) | Spiegelt B2/B3/B4/B5-Split: der Orchestrator hält Broker + Worktree-Primitiv + Human-Resolver (Execution-Layer-Sorgen, die nie serialisieren). Ein Status-Write aus untrusted Ticket-Daten muss human-gegatet sein, nicht RPC-gefeuert. |
| RPC-Fläche / Wiring | **`registerTaskForge` (`sidecar/register-task-forge.ts`)** registriert `task` (read-only) + `forge` (read-only) über die Wire; in `createSidecar` (main.ts) **und** TS-IPC-Harness | angewendet (B6) | `listTickets`/`myTickets`/`nextFromSprint`/`whoseTurn`/`stale` sind JSON-shaped und serialisieren über den Socket — über den echten Spine verifiziert (`task-forge-ipc.test.ts`). Default-Boot: jira+github; linear/gitlab per Option. |
| Untrusted-Egress per-call only | **Resolving an `ask` whose ORIGINAL request was untrusted egress can never persist a `session`/`scoped` grant** — the policy engine (the chokepoint) clamps it to a single-use consumable grant; a `deny` may still persist (sticky deny is safe) | angewendet (security hardening) | untrusted egress is per-call only — never persistable as session/scoped; resolved 2026-06-21 security hardening. §3.3 lethal-trifecta: a human `session`/`scoped` OK on an untrusted-derived egress previously laundered into a standing grant that auto-cleared FUTURE trusted egress of the same kind/scope. Now `resolve` records a single-use grant `decide` allows exactly once then drops — the one approved call still executes, a subsequent untrusted egress re-asks, and no trusted egress is pre-cleared. Verified (`broker.test.ts`, `acp-transport.test.ts`). |
| Red-team Fix 1 — Client-assigned taint | **The CLIENT (`AcpSession`) assigns egress taint by session provenance, not the agent.** A session carries a `#untrusted` flag (default `true`; ToDo/prompt/web/ticket-derived sessions are untrusted). Per request the effective taint = `sessionUntrusted || request.fromUntrusted` — the agent's `fromUntrusted` may only ESCALATE, never downgrade | angewendet (security hardening 2026-06-21) | Previously `fromUntrusted` was taken verbatim from the agent's own `session/request_permission` payload (`acp-session.ts`), so a hostile agent could send `fromUntrusted:false` on an egress to dodge the lethal-trifecta gate. Now the client owns the taint; a downgraded egress in an untrusted session is still forced to `ask`. Files: `sidecar/acp/acp-session.ts` (`#untrusted` field + `effectiveUntrusted`). Verified (`acp-transport.test.ts` "Fix 1" + negative-control), regression fixture `sidecar/acp/malicious-stub-acp-agent.mjs`. |
| Red-team Fix 2 — Sealed ACP subprocess | **`AcpTransport` spawns the agent with stderr `pipe` (captured to a buffer, never `inherit`) and an EXPLICIT minimal env allowlist (`PATH`/`HOME`/`TMPDIR`), never a `process.env` spread.** No credential ever reaches the child's env/argv/stderr — the broker injects secrets only at the execution layer | angewendet (security hardening 2026-06-21) | A real agent CLI under the old `stdio:["pipe","pipe","inherit"]` + inherited env could leak a host key via stderr or env. `sealedChildEnv()` + `SEALED_CHILD_ENV_ALLOWLIST` exported; `childEnv`/`stderr` getters expose the sealed state for assertion. Files: `sidecar/acp/acp-transport.ts`. Verified (`acp-transport.test.ts` "Fix 2"). |
| Red-team Fix 3 — Bind execute to authorize (C3) | **`authorize` mints a single-use `ExecutionGrant` (opaque id) bound to the exact (principal × request) fingerprint when it decides `allow`; `execute` REQUIRES a matching, unconsumed grant and consumes it.** `execute` is no longer an independent re-decide | angewendet (security hardening 2026-06-21) | Previously `execute` re-decided over a caller-shaped request and `decide` ignored the principal, so a caller could call `execute()` with a forged "trusted" request and bypass the gate. Now no grant exists for a forged request → rejected with `not authorized`. The human-laundering path obtains a fresh grant via a human-authority `authorize`. Files: `src/contracts/broker.ts` (`ExecutionGrant`, `BrokerDecision.grant`, `execute` options), `sidecar/broker/capability-broker.ts` (`#grants` registry + `requestFingerprint`), `sidecar/acp/acp-session.ts`, `sidecar/task-forge/ticket-lifecycle.ts`. Verified (`broker.test.ts` C3 trio + `acp-transport.test.ts`/`ticket-lifecycle.test.ts` still green). |
| Red-team Fix 4 — Freeze WriteEscape (S3) | **`WriteEscape` is `Object.freeze`d at construction with an opaque one-time `id`; consumption is tracked by a broker-side used-escape registry (`#usedEscapes`, keyed on `id`), not by the mutable `consumed` flag.** A caller can neither reset `consumed` nor re-mint a clone to replay a prod write | angewendet (security hardening 2026-06-21) | The escape was a mutable plain object whose `consumed` could be reset, and a supplier could mint a fresh escape per call to replay a prod write. Freezing + an id-keyed registry make both replays fail. Files: `src/contracts/tooling.ts` (`makeWriteEscape` freeze + `id`), `sidecar/broker/capability-broker.ts` (`#usedEscapes`). Verified (`broker.test.ts` prod-write trio, `backend-contracts.test.ts`). |
| Red-team Fix 5 — Target-bind untrusted-egress consumable (S2) | **The policy engine's single-use consumable grant is keyed by `kind+target(+command)`, not `kind:scope` alone.** Approving untrusted egress to target A clears ONLY that exact call — never a different untrusted egress to target B | angewendet (security hardening 2026-06-21) | The consumable was keyed `${projectKey}:${kind}:${scope}`, so approving egress to A cleared any same-kind egress to B. Now `consumableKey(request, scope)` binds it to the concrete target. Files: `sidecar/broker/policy-engine.ts` (`consumableKey`). Verified (`broker.test.ts` "Fix 5"). |

## Dev WebSocket bridge (B7 / road-to-runnable-dev P0)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Dev-Transport-Wahl | **Localhost-WebSocket-Brücke** (`sidecar/dev-bridge/`), **dev-only**, bindet **nur `127.0.0.1`**; Prod-Pfad bleibt der Tauri-Unix-Socket (deferred) | angewendet (P0) | Der Browser kann den Unix-Socket nicht sprechen → `pnpm dev` zeigte nur Mocks. Die WS-Brücke frontet dieselbe IPC (JSON-RPC/NDJSON) über denselben `ProviderRegistry`/`IpcConnection`-Spine. Klar als „NOT FOR PRODUCTION" markiert; kein Tauri/cargo. |
| WS-Impl ohne neue Dep | **Hand-gerollter RFC6455-Server** (`dev-bridge/ws-server-transport.ts`, Text-Frames, `node:http`+`node:crypto`) + browser-seitiger `WsClientTransport` (`src/lib/sidecar/client/ws-client-transport.ts`, Plattform-`WebSocket`) | angewendet (P0) | Projekt-Posture „keine neue Dependency" (wie B1 System-`git`). `ws` ist nicht installiert; eine kleine, dev-only Framing-Schicht (Text-Frames, kein permessage-deflate) reicht für JSON-RPC-Zeilen. Beide Transports erfüllen das bestehende `Transport`-Interface → exakt dieselben Peer-Codepfade wie der Unix-Socket. |
| Vite-Dev-Injection | **`src/lib/dev/connect-dev-bridge.ts`** verbindet die WS-Brücke und ruft `installSidecarBridge(transport)` **vor** dem ersten `getProviders()` (memoisiert) in `main.tsx`, gegated auf `import.meta.env.DEV` | angewendet (P0) | `getProviders()` nimmt die echten IPC-Proxies, sobald `__CAPISCO_SIDECAR__` injiziert ist. Brücke aus/unerreichbar (`VITE_CAPISCO_DEV_BRIDGE=off`) → still auf Mocks zurück (App bootet immer, degradiert, nie kaputt). Prod-Build tree-shaket den Pfad (`if (import.meta.env.DEV)`). |
| Concurrent `pnpm dev` | **`scripts/dev.mjs`** spawnt Brücke + Vite (zeilen-präfixt, Ctrl-C tears beide), dep-frei (kein `concurrently`); `dev:ui`/`dev:sidecar` als Einzel-Targets | angewendet (P0) | Akzeptanz „`pnpm dev` startet beides". Node 25 fährt die `.ts`-Sidecar-Entry direkt (type-stripping). |
| `@/`-Alias auf bare Node | **`scripts/alias-loader.mjs` (ESM-resolve-Hook) + `scripts/register-alias.mjs`**, via `node --import` geladen: mappt `@/`→`src/` UND löst extensionlose relative/Alias-Imports bundler-style (`.ts`/`.tsx`/`/index.ts`) | angewendet (P0) | vitest/vite lösen den Alias + extensionlose `src/`-Imports; bare `node sidecar/...` nicht (der Sidecar-Standalone-Run lief de facto nie). Dep-frei. node_modules/builtins fallen durch zum Default-Resolver. Verifiziert: `pnpm dev:sidecar`-Pfad bootet standalone + ein echter WS-Client liest echten Baum (Marker `M`) + echten Inhalt aus einem Temp-Repo. |
| Fallback bleibt intakt | **Playwright (visual) + vitest laufen OHNE Brücke gegen Mocks**; Playwright `webServer` ist `pnpm build && pnpm preview` (Prod, `DEV=false`) → Brücke nie aktiv, Goldens byte-identisch | angewendet (P0) | Harte Anforderung: nur die Dev-Runtime injiziert die WS-Brücke. 83 visual specs + 332 vitest grün ohne Änderung. |
| Security-Architektur-Test (Review must-fix) | **`sidecar/test/broker-chokepoint.test.ts`** scannt jede Nicht-Test-`sidecar/**`-Datei und **failt**, wenn `child_process`/fs-Write/`fetch`-Egress **außerhalb** der allowlisteten Execution-Primitive auftaucht | angewendet (P0) | Akzeptanz „FS/Shell/Netz nur durch broker-mediierte Pfade; denied call ⇒ kein Seiteneffekt". Allowlist = die auditierten Primitive (`git/git-exec`, `quality/real-quality-provider`, `acp/acp-transport`, `recent/recent-projects`, `fs/fs-exec`) + Socket-Lifecycle (`server/sidecar`). Der Test hat Zähne (injizierter `spawn` außerhalb der Allowlist failt; entfernt → grün). Test-Tree + Rust-Stub out-of-scope. Egress = `fetch(`/`node:https` (Lethal-Trifecta-Sorge); `node:http`-Server (inbound, loopback) + `node:net`-Unix-Socket (IPC-Spine selbst) sind kein arbitrary Egress. |

## Projekt öffnen + echter Explorer (B7 / road-to-runnable-dev P1)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| FS-Tree-Contract-Ort | **`contracts/fs-tree.ts`** (`ProjectFsProvider`/`FsTreeNode`/`OpenedProject`/`FileContent`) — jede Methode per Repo-Root-`path` gekeyt (wie `GitOpsProvider`s `cwd`) | angewendet (P1) | Der echte Explorer braucht einen rekursiven On-Disk-Baum + echten Datei-Inhalt — beides **fs-Reads**. Path-keyed ⇒ eine Instanz bedient jedes zur Laufzeit geöffnete Projekt ohne Re-Registrierung. Re-exportiert via `contracts/index.ts`. |
| FS-Read-Primitive (Chokepoint) | **`sidecar/fs/fs-exec.ts`** = der einzige, allowlistete Ort für `node:fs`-Reads (tree/content) mit **Path-Traversal-Guard** (`safeResolve` rejected `..`-Escape aus dem Root) | angewendet (P1) | Posture wie `git-exec.ts`: das first-party Read-Primitiv, das der Broker mediiert — kein offener fs-Escape. Vom Architektur-Test als auditiertes Primitiv gelistet. Ein relativer Pfad kann nie aus dem geöffneten Projekt ausbrechen (Test). |
| Real-FS-Provider | **`RealFsProvider` (`sidecar/fs/real-fs-provider.ts`)** baut den rekursiven Baum (Dirs zuerst, alphabetisch, Ignore-Set `.git`/`node_modules`/build-dirs, `MAX_NODES`-Cap) + faltet Git-Marker aus `git status`; `readFile` über `fs-exec` | angewendet (P1) | Akzeptanz „echter Datei-Baum (echte Git-Marker, virtualisiert)". Reads ausschließlich über das Primitiv. Mock (`src/mocks/fs-tree.ts`) bleibt der Browser/Visual-Fallback (deterministisch, disk-frei). |
| Mock bleibt Default | **`projectFs` Default-Boot = `mockProjectFsProvider`** (register-mocks); der Dev-Bridge swappt den echten `RealFsProvider` (`register-dev-workspace.ts`, `registry.replace`) | angewendet (P1) | Unix-Socket-Sidecar + Browser bleiben deterministisch; der reale fs-Provider ist der dünne Dev-Bridge-Swap hinter derselben Wire-Id. `ProviderRegistry.replace` neu (additiv) für den Override. |
| UI-Wiring (additiv, Goldens intakt) | **Default (kein Projekt offen) = bestehende mock-getriebene Explorer-Sicht unverändert**; ein zur Laufzeit geöffnetes Projekt (Pfad-Eingabe `OpenProjectBar`) flippt auf `RealProjectTree` (live Baum + Git-Marker, virtualisiert); File-Klick lädt **echten** Inhalt via `useEditor.openRealDoc` | angewendet (P1) | Die visual specs (`git-near`/`shell-chrome`) asserten die mock-getriebene Sicht und der Editor-Spec spezifische Mock-Inhalte → die müssen byte-identisch bleiben. Die Visual-Harness installiert nie eine Brücke und öffnet nie ein Projekt → rendert immer die Default-Sicht. Zustand: `src/shell/open-project-store.ts` (zustand). |
| Editor real content | **`useEditor.realDocs` (relPath→`RealDoc`)**; `EditorWorkspace` bevorzugt einen Real-Doc über den Mock-Snapshot, Mock-Overlays (blame/presence/inlay) bleiben für einen Real-Doc leer (ehrlich; LSP später) | angewendet (P1) | „Klick auf Datei lädt echten Inhalt in den Editor". CM6 indiziert nur `doc.text` — ein Real-Doc erfüllt dieselbe `EditorDoc`-Form. Mock-Pfad unverändert wenn kein Real-Doc (immer so in der Visual-Harness). |
| P1-UI-Integrationstest crosst Runtime-Grenze | **`*.node-int.test.tsx`** (rendert React **und** stellt einen echten Sidecar-Registry über In-Process-Pipe + Temp-Repo auf) ist aus `tsconfig.app.json` **ausgeschlossen** (Browser-pure), läuft aber unter vitest (jsdom liefert DOM + node-Builtins) | angewendet (P1) | Der Test braucht bewusst beide Welten zur Laufzeit. Ein Import von node-Builtin-`sidecar/`-Dateien in das browser-getypte App-tsc-Projekt würde dessen Reinheit brechen; die App bleibt node-frei getypt. Verifiziert grün durch den vitest-Gate (open→tree→git-marker→click→real content). |

## Editor lesen/schreiben + echter perform-Adapter (B7 / road-to-runnable-dev P2)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| FS-Write-Primitive (Chokepoint) | **`sidecar/fs/fs-write-exec.ts`** = der einzige, allowlistete Ort für `node:fs`-WRITES (`writeFileSync`/`mkdirSync`) mit Path-Traversal-Guard (reused `safeResolve`); **nie** direkt von einem Provider importiert — einziger Caller ist der broker-mediierte Perform-Adapter | angewendet (P2) | Posture wie `git-exec.ts`/`fs-exec.ts`: das first-party Write-Primitiv, das der Broker mediiert. Ein denied call erreicht die Datei nie → kein Disk-Change. Vom Architektur-Test als auditiertes `fs-write`-Primitiv gelistet. |
| Echter `perform`-Adapter (der Stub war ein No-op) | **`sidecar/fs/fs-write-broker.ts` (`BrokerFsWriter`)** komponiert `broker.authorize → [ask → human-gate] → broker.execute(perform = fs-write)` — der Disk-Write läuft **ausschließlich** in der `execute`-Callback (Chokepoint), mit Single-Use-Grant (C3) + Audit-vor-Ausführung | angewendet (P2) | Schließt das Security-„must-fix": jeder reale Seiteneffekt nur durch `broker.execute`. Ein Save ist HUMAN-initiierte, **trusted** Intent (kein `fromUntrusted` → kein Lethal-Trifecta-Gate). Spiegelt den `AcpSession`-Broker-Seam, wiederverwendet für einen Datei-Save. |
| File-Write = `ask` (Default-Config unverändert) | **`file-write` bleibt `ask`** in `DEFAULT_GRANT_CONFIG`; der Dev-Workspace clearet den Gate **zur Laufzeit** über den §3-Rückkanal (`resolvePermission: () => ({axis:"session"})`) — **keine** Config-Aufweichung | angewendet (P2) | §4 human-gated: der Build erfindet keine weite Allowlist. Ein Save ist die Mensch-Entscheidung „ja, schreib das" — als Return-Channel-Decision pro Session, nicht als hartcodierter `allow`. |
| `projectFs.writeFile` auf der Wire (Execution in-process) | **`ProjectFsProvider.writeFile(path,relPath,text) → FileWriteResult`**: Pfad+Inhalt (der Editor-Buffer, **kein** Secret) crossen die Wire; die Broker-EXECUTION läuft in-process im `RealFsProvider` | angewendet (P2) | Der Save-Inhalt ist der User-Buffer, serialisierbar. Der Secret-/Execute-Pfad bleibt off-wire (spiegelt B4). `RealFsProvider` nimmt einen optionalen `BrokerFsWriter`; ohne Writer (read-only Boot) ⇒ `{written:false}`, kein Disk-Touch. Mock-`writeFile` = disk-freier No-op (`written:false`). |
| CodeMirror editierbar (Mock bleibt read-only) | **`CodeMirrorView` wird editierbar, wenn `onChange` übergeben ist** (ein Real-Doc); `updateListener` pusht den Buffer in den Store, `Mod-s` feuert `onSave`. Ohne `onChange` (Mock-Snapshot, immer in der Visual-Harness) bleibt die Sicht read-only | angewendet (P2) | „CodeMirror editierbar; dirty/save-Flow". Die Pixel-Goldens asserten den Mock-Pfad → der bleibt read-only + byte-identisch. Real-Docs tragen `savedText` (dirty = `text !== savedText`); `useEditor.saveActive` routet über `projectFs.writeFile`. Save-Bar (`editor-save`/`editor-dirty`/`editor-saved`/`editor-save-gated`) nur für Real-Docs gerendert. |
| Architektur-Test: `writeFile`-Methodenname ≠ fs-write | **Die `fs-write`-Patterns matchen die echten `node:fs`-Write-CALLS (`*Sync`-Formen, die der Sidecar nutzt) + `fs.<op>(`-Promise-Formen** — nie einen Provider-METHODENNAMEN `writeFile` (der an den Broker delegiert) | angewendet (P2) | `ProjectFsProvider.writeFile` ist eine Contract-Methode, kein fs-Seiteneffekt — der bare `/\bwriteFile\(/`-Match war ein False-Positive. Der Test behält Zähne: ein injiziertes `writeFileSync` außerhalb der Allowlist failt weiterhin (verifiziert: Probe-Datei rot, entfernt grün). |
| P2-Verifikation (denied ⇒ kein Seiteneffekt) | **`fs-write-broker.test.ts` + `fs-write-ipc.test.ts` + `EditorSave.node-int.test.tsx`**: (a) gecleared → echter Disk-Change + `executed`-Audit; (b) deny-all → Datei byte-identisch, **kein** `executed`-Audit; (c) Path-Traversal rejected; (d) über die Wire (WS/Pipe); (e) UI edit→dirty→Save→Disk | angewendet (P2) | Hermetisch gegen Temp-Repos. Schließt den Review-„must-fix"-Beweis: „denied capability ⇒ kein Seiteneffekt", end-to-end + auf jeder Ebene (Primitive, Provider, Wire, UI). |

## Worktree + Session live (B7 / road-to-runnable-dev P3)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Worktree-Ops immer real im Dev-Bridge (path-keyed) | **`registerDevWorkspace` registriert `worktree-ops` IMMER real** (`RealWorktreeProvider`, path-keyed über `repoCwd`) — wie `projectFs` — egal ob ein Default-Repo konfiguriert ist | angewendet (P3) | Akzeptanz „Worktree anlegen/wechseln gegen das offene Repo". Eine Instanz bedient jedes zur Laufzeit geöffnete Projekt. Der git/workspace-Swap bleibt repo-gebunden (`cwd`); Worktree-Ops sind path-keyed → kein Re-Registrieren beim Öffnen. |
| Bundle-Erweiterung (`worktree`/`session`/`todo`) | **`ProviderBundle` bekommt `worktree`/`session`/`todo`** als IPC-Proxies (Wire-Ids `worktree-ops`/`session`/`todo`) + browser-sichere Mocks (`src/mocks/worktree.ts`, `src/mocks/session.ts`) | angewendet (P3) | Die UI braucht browser-seitigen Zugriff auf das live Primitiv. Alle Methoden sind async → reine RPC-Proxies. Mocks: Worktree = in-memory; Session = in-memory; `todo.sendToAgent` rejected im Browser (kein Broker/ACP) — der live Run ist der Dev-Bridge-Swap. Keine neue Visual-Fläche (Panel nur bei offenem Projekt → nie in der Harness). |
| Worktree-UI dev-runtime-only (Goldens intakt) | **`WorktreePanel`** (`src/shell/worktree-store.ts` + `src/shell/panels/WorktreePanel.tsx`) nur gemountet, wenn ein Projekt offen ist (im Explorer, neben `RealProjectTree`); list/create(neuer Branch)/switch/start-session | angewendet (P3) | Wie P1/P2: die Visual-Harness öffnet nie ein Projekt → das Panel rendert nie → Pixel-Goldens byte-identisch. Worktree-Pfade default `.capisco-worktrees/<branch>` (im Ignore-Set, GC-freundlich). |
| ToDo→Agent live: Default fail-closed | **Der Dev-Bridge `registerSession` bleibt fail-closed** (deny-all Resolver); der cleared ToDo→Agent-Pfad wird im Integrationstest mit explizitem human-clearing Resolver exerziert (repräsentiert den User-OK) | angewendet (P3) | §3.3: der Agent-abgeleitete Egress ist untrusted — nie auto. Eine reale UI swappt einen Prompt-Resolver. Der Test beweist den **gecleared** Pfad gegen ein echtes Temp-Repo-Worktree, ohne die sichere Default aufzuweichen. |
| P3-Verifikation (live Spine) | **`worktree-session-live.test.ts` + `WorktreePanel.node-int.test.tsx`**: (a) echtes `git worktree add` über die Wire; (b) Session coupled an das Worktree; (c) ToDo→Agent (broker-gegateter Stub) im **echten** Worktree → `done`, streamt in den resumable Store; (d) remove; (e) UI: open→Panel→create(real on disk)→start session | angewendet (P3) | Hermetisch gegen Temp-Repos. Beweist die volle P3-Spine end-to-end + auf UI-Ebene. Spiegelt den bestehenden `worktree-ipc`/`todo-agent`-Beweis, erweitert um den Dev-Bridge-/UI-Pfad. |

## Echter ACP-Agent — key-gated (B7 / road-to-runnable-dev P4)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Adapter = Resolution-Layer, kein neuer Spawn-Pfad | **`sidecar/acp/real-acp-config.ts`** löst `{command,args,model,credentialRef}` aus Config/Env auf; der bestehende `AcpSession`/`AcpTransport`-Spawn (sealed subprocess + client-assigned taint) bleibt **unverändert** | angewendet (P4) | §5 Deferred: echte CLI = dünner Swap über dieselben stdio-Frames. Der Adapter beantwortet nur „ist ein echter Agent JETZT lauffähig?" und gibt sonst `undefined` zurück → Stub bleibt Default. |
| Key-gated, nicht erfunden | **LIVE nur wenn BEIDES gilt:** (1) CLI ist konfiguriert **und** auf der Platte installiert (`resolveCliPath`), (2) Key ist im Broker-Vault präsent. Sonst dormant (`no-cli-configured` / `cli-not-installed` / `key-not-available`) | angewendet (P4) | Harte Regel: kein eingebackener Default. Eine konfigurierte-aber-nicht-installierte CLI failt nicht zur Laufzeit — sie bleibt dormant. Der echte LLM-Run bleibt deferred bis User Key + CLI liefert. |
| Secrets credentialRef-only | **Der Roh-Key wird EINMAL in `resolveRealAcpAdapter` gelesen und sofort via `broker.secrets.put(ref, …)` in den Vault verschoben.** Danach reist nur der Referenz-NAME; der Wert wird nie zurückgegeben, nie in Child-Env/argv/Prompt/Audit | angewendet (P4) | Broker §3.2 capability-by-reference. `SEALED_CHILD_ENV_ALLOWLIST` ist credential-frei; die echte CLI liest ihren Key aus eigenem Keychain/Config. |
| Verifikation gegen den Stub | **`real-acp-adapter.test.ts`**: dormant-Default + 3 dormant-Reasons + LIVE-Pfad (CLI installiert + Key) + credentialRef-only + env-Reading; der LIVE-Pfad treibt einen echten broker-gegateten ACP-Run (CLI = `node stub-acp-agent.mjs`) mit identischer deterministischer Spine + voll-auditiert | angewendet (P4) | Beweist den Swap-Pfad end-to-end ohne echten LLM/Key. Ein echter `claude-code`/`codex`/`gemini`-Run ist derselbe Code-Pfad mit anderer Binary. |

### Den echten ACP-Agent scharfschalten (wo Key/CLI hin)

Dev-Bridge / Sidecar lesen die Config aus Env (`readRealAcpEnv`). Setze **vor** dem Start:

- `CAPISCO_ACP_CLI` — die ACP-fähige Agent-CLI (z. B. `claude-code`, `codex`, `gemini`). Bare-Name → über `PATH` aufgelöst; absoluter Pfad → direkt. **Pflicht**, sonst dormant.
- `CAPISCO_ACP_CLI_ARGS` — optionale Extra-Args (whitespace-getrennt), z. B. `--acp`.
- `CAPISCO_ACP_MODEL` — optionales Transcript-Label (Default = CLI-Name).
- `CAPISCO_ACP_API_KEY` — optionaler Roh-Key. Wird **einmal** in den Broker-Vault verschoben; danach nur noch der Referenz-Name. Alternativ den Key vorab via Keychain/`broker.secrets.put` unter `CAPISCO_ACP_CRED_REF` ablegen → Adapter geht ohne erneutes Roh-Wert-Liefern live.
- `CAPISCO_ACP_CRED_REF` — Referenz-Name des Keys (Default `acp-agent-key`).

Ohne `CAPISCO_ACP_CLI` (oder bei nicht-installierter CLI / fehlendem Key) bleibt der deterministische Stub der Default — Tests/Goldens/CI brauchen nie einen echten Agent.

### Re-Audit-Checkliste vor dem Live-Schalten

1. **Sealed subprocess** intakt: `SEALED_CHILD_ENV_ALLOWLIST` enthält **keinen** Credential-Var; stderr ist `pipe` (nie `inherit`). (`acp-transport.test.ts` "Fix 2")
2. **Client-assigned taint** intakt: die Session ist default-untrusted; ein Agent-`fromUntrusted:false` darf **nicht** downgraden — Egress bleibt hart gegated. (`acp-transport.test.ts` "Fix 1")
3. **Broker-Chokepoint** intakt: kein `child_process`/`fetch`/fs-write außerhalb der allowlisteten Execution-Primitives. (`broker-chokepoint.test.ts`)
4. **Secrets** bleiben referenz-only: keine `secrets.get(ref): string`-Methode; der Key taucht nie in Audit/Log/Prompt auf. (`real-acp-adapter.test.ts` + Vault-Shape)
5. **Human-Gate** bleibt fail-closed: der untrusted-abgeleitete Egress des echten Agents geht nie auto — nur ein per-call Human-OK (oder ein UI-Resolver) klärt ihn.
6. **Prod-Datasources / Default-Grant-Allowlist**: weiterhin human-confirmed, nie aus der echten CLI inferiert. Vor Live-Schalten bestätigen, dass die Default-Config nicht versehentlich aufgeweitet wurde.

## Agent-Provisioning — Backend-Detection + broker-gegateter Install (B8 P0/P1)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Provisioning-Contract-Ort | **`contracts/provisioning.ts`** (`AgentBackend`/`AgentBackendStatus`/`AgentBackendDriver`/`InstallOutcome`/`BackendProvisioner`) | angewendet (B8 P0) | Strukturierter Backend-Katalog mit `status = ready\|installable\|guide` + `installCommand?`/`guideUrl?`/`version?`/`driver`. Alle Felder secret-frei → wire-/log-/display-safe. Re-exportiert via `contracts/index.ts`. Zwei Claude-Backends modelliert (`native-stream-json` direkt, `acp-bridge` via Zed-Bridge), erweiterbar auf codex/gemini. |
| Detect = Read-only Primitive (Chokepoint) | **`sidecar/provision/detect-exec.ts`** = der einzige auditierte Ort für die **read-only** `which`/`--version`-Probes (`execFile`, **nie** Shell), mit **Mutating-Arg-Guard** (refuse `install`/`i`/`add`/`run`/…) | angewendet (B8 P0) | Posture wie `git-exec.ts`: das first-party Read-Primitiv, das der Broker mediiert — kein offener Shell-Escape. Der Guard stellt strukturell sicher, dass das Probe-Primitiv nie als Install-Pfad zweckentfremdet wird (Installs gehen ausschließlich über `install-exec.ts`). Als auditiertes `process`-Primitiv im Architektur-Test gelistet. |
| Provisioner-Impl | **`RealBackendProvisioner` (`sidecar/provision/backend-provisioner.ts`)** baut den Katalog: node/npm/npx (prereq), `claude` (native), `claude-code-acp` (bridge), optional codex/gemini; injizierbare `HostProbe` (Default = reales Primitiv, Tests = Fake-Tabelle → hermetisch) | angewendet (B8 P0) | Akzeptanz „Detection läuft echt auf dieser Maschine". `claude` fehlt → **`guide`** (CLI trägt den Login → **nie** Auto-Install); Bridge fehlt + npm da → **`installable`** mit exakter `["npm","i","-g","@zed-industries/claude-code-acp"]`; Bridge + npm fehlen → `guide` (erst node/npm). Deterministisch über Fake-Probe getestet (`backend-provisioner.test.ts`). |
| Install = Mutating Primitive (Chokepoint) | **`sidecar/provision/install-exec.ts`** = der einzige auditierte Ort für den **mutating** Install-Spawn (`execFile`, **nie** Shell); **einziger Caller** = der broker-gegatete `BrokerInstaller` | angewendet (B8 P1) | Posture wie `fs-write-exec.ts`: das mutating Primitiv läuft **ausschließlich** in `broker.execute`. Ein denied Call erreicht es nie → kein Install. Als auditiertes `process`-Primitiv im Architektur-Test gelistet. Wirft nie auf non-zero Exit — der Caller meldet `installed:false` (nie stiller Erfolg). |
| Broker-gegateter Installer | **`sidecar/provision/install-broker.ts` (`BrokerInstaller`)** komponiert `broker.authorize(shell) → [ask → human-gate] → broker.execute(perform = runInstall)` — Single-Use-Grant (C3) + Audit-vor-Ausführung | angewendet (B8 P1) | Akzeptanz „Install broker-gegatet, nie still". Spiegelt den `BrokerFsWriter`-Seam, wiederverwendet für einen Install. Ein Install ist **HUMAN-initiierte, trusted** Intent (User klickt das exakte Kommando) → **kein** `fromUntrusted` (kein Lethal-Trifecta-Gate; das Kommando ist die literale detected argv, nicht aus untrusted Agent/Web abgeleitet). Default-Resolver **fail-closed (deny-all)**; eine reale UI swappt einen „bestätige das exakte Kommando"-Prompt. |
| Default-Grant-Config (unverändert weit) | **`npm i*`/`npm install*`/`npx*` als `ask`** in `DEFAULT_GRANT_CONFIG`; `sudo*`/`rm*` bleiben `deny` | angewendet (B8 P1) | §4 human-gated: der Build erfindet **keine** weite Allowlist — Installs sind mutating → immer `ask`, nie auto-`allow`. Der `sudo`-Prefix eines Installs trifft weiterhin die Deny-Regel (Test: `sudo npm i -g …` → broker-denied, kein Kommando). Fail-closed bleibt: ein Kommando ohne Regel defaultet im Engine zu `ask`. |
| RPC-Fläche / Wiring | **`registerProvision` (`sidecar/register-provision.ts`)** registriert `provision` (`detect` + `install`) über die Wire; in `createSidecar`/`registerAllProviders` (main.ts) **hinter** dem Broker | angewendet (B8 P0/P1) | `detect` ist read-only + secret-frei → wire-safe (über echten Socket-Spine verifiziert, `provision-ipc.test.ts`). `install` delegiert an den in-process `BrokerInstaller` — der Gate + die Ausführung bleiben im Chokepoint (kein RPC-gefeuerter Seiteneffekt; spiegelt B4/P2-Split). Ohne Broker gewired → `install` meldet `installed:false` „no broker" (nie still). |
| Verifikation (autonom; echter Install deferred) | **Hermetisch** über Fake-Probe (Detection) + Fake-Runner/Real-DRY-`echo` (Install). Echter `npm i -g` + echter Agent-Run = **User-broker-approved go** | angewendet (B8 P0/P1) | Hard-Rule: nichts global installiert, kein bezahlter Agent-Call, kein npx-Fetch im Build. Der gecleared Install-Pfad ist mit einem **DRY/echo**-Kommando durch den **echten** `runInstall`+Broker end-to-end bewiesen (`install-broker.test.ts`/`provision-ipc.test.ts`); der deny-Pfad beweist „denied ⇒ kein Kommando, kein `executed`-Audit". Architektur-Test grün mit den zwei neuen `process`-Primitiven (`detect-exec`/`install-exec`). |

## Native Claude-Code-Adapter — stream-json (B8 P2a)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Adapter = nativer Spawn-Pfad neben ACP | **`ClaudeCodeProvider` (`sidecar/acp/claude-code-provider.ts`)** — der **native** Bruder von `AcpSession`: gleiche Session-Fläche (`start` + `subscribe`-Event-Kanal), gleicher un-bypassbarer Broker-Seam, aber treibt den **stream-json**-Transport statt ACP-stdio | angewendet (B8 P2a) | Befund: `claude` (Claude Code 2.x) spricht **kein** ACP, sondern den SDK-Stream-JSON-Modus (`claude -p --output-format=stream-json --input-format=stream-json --verbose`). Der native Adapter treibt das direkt; der ACP-Weg (P2b, Zed-Bridge) bleibt der zweite Backend. Beide hinter derselben Session-Interface wählbar (`registerSession({backend})`). |
| Drei-Teilung (Primitive / Parser / Driver) | **`claude-stream-exec.ts`** (sealed Child-Spawn) + **`stream-json-parse.ts`** (purer Parser) + **`claude-code-provider.ts`** (Broker-Seam + Run-Loop) | angewendet (B8 P2a) | Spiegelt B1/B5 (`git-exec`/`git-parse`, `real-quality-provider`/`quality-parse`): der `child_process`-Spawn lebt im allowlisteten Transport-Primitiv (Architektur-Test), der Parser ist DOM-/Prozess-frei und unit-getestet auf der recorded Fixture, der Driver hält den Broker-Gate + Store. |
| Stream-JSON → SessionEvent-Mapping | **`parseStreamJsonEnvelope`** mappt `system/init`→`status:running`, `assistant`-`text`→`token`-Delta, `assistant`-`tool_use`→broker-routbarer `AcpToolCall` (**kein** `tool`-Event bis der Broker freigibt), `result`→`telemetry`+`done` (+`status:error` bei `is_error`) | angewendet (B8 P2a) | Dieselbe `SessionEvent`-Union die Mock/Stub/ACP emittieren → ein UI-Consumer weiß nicht, welches Backend spricht. `user` (tool_result-Echo) trägt **kein** Event: der Tool-Block wird erst beim Broker-`execute` aufgenommen (nicht aus dem CLI-Echo — das würde doppelt zählen / am Gate vorbei). |
| Konservative Tool-Klassifikation | **`toolKind`**: Read/Glob/Grep/NotebookRead→`file-read`, Write/Edit/MultiEdit/NotebookEdit→`file-write`, WebFetch/WebSearch→`network`, Bash→`shell`; **unbekannt/MCP/custom→`shell`** (engste, immer-`ask`-Lane) | angewendet (B8 P2a) | Ein neues/unbekanntes Tool darf nie in eine permissivere Kind mis-klassifiziert werden — Default ist die meist-gegatete (`shell`). `toolTarget` zieht den non-secret-`target` aus dem Input (file_path/command/url/pattern), nie ein Secret. |
| Broker-Gate (Invariante, identisch zu AcpSession) | **Jeder `tool_use` durch den Broker**: `authorize` (agent, untrusted, audited) → Human-Gate bei `ask` → `execute` als `human` über die trusted Request-Form (§3.3-Laundering, C3-Single-Use-Grant). Nur ein gecleared Gate führt das Side-Effect in `broker.execute` aus und nimmt den Tool-Block auf; deny/blocked wird als `<title> (blocked)` ehrlich im Transcript vermerkt (nie still gedroppt) | angewendet (B8 P2a) | Byte-für-Byte-Analog zu `AcpSession.#onAgentRequest`. „Keine Agent-Capability ohne Broker-Call" gilt für den nativen Pfad genauso. |
| Client-assigned Taint (Red-team Fix 1) ohne Per-Call-Flag | **Die Session trägt die Egress-Taint (`#untrusted`, Default `true`)** — das stream-json-Protokoll hat **keinen** model-kontrollierten `fromUntrusted`-Kanal, also ist die effektive Taint rein die Session-Provenance | angewendet (B8 P2a) | Ein nativer Run ist by-provenance untrusted (wie ACP); der model-abgeleitete Egress (das Edit) trifft den Lethal-Trifecta-Gate. Da das Protokoll kein Downgrade-Vektor hat, kann ein hostiles Model die Taint nicht senken — strukturell sicherer als der ACP-Per-Call-Flag (dort escaliert der Flag nur). |
| Sealed Subprocess (Red-team Fix 2) + **kein roher Key** | **`ClaudeStreamTransport` reused `sealedChildEnv()`** (credential-freie Allowlist `PATH`/`HOME`/`TMPDIR`, kein `process.env`-Spread) + stderr `pipe` (nie `inherit`); `cwd` nur gesetzt wenn der Pfad existiert (sonst ENOENT beim Spawn) | angewendet (B8 P2a) | Der native Pfad nutzt den **bestehenden `claude`-Login** — die CLI liest ihren eigenen Auth aus eigener Config/Keychain. **Kein** Key wird eingeführt, keiner reist in Child-Env/argv/stderr. Re-Audit-Checkliste (ACP P4) gilt unverändert. |
| Verifikation (recorded Fixture, kein echter `claude`) | **`stream-json-fixture-agent.mjs`** replayt die committed `fixtures/claude-stream-json.fixture.jsonl` über dasselbe stdout-Protokoll — der Adapter unterscheidet es nicht vom echten CLI | angewendet (B8 P2a) | Hard-Rule: kein bezahlter `claude`-Call. Voller Spine bewiesen (`claude-code-provider.test.ts`): (a) deterministischer Transcript + `done`; (b) jedes Tool durch den Broker-Audit; (c) **denied ⇒ kein Side-Effect, kein `executed`-Audit, `(blocked)` im Transcript**; (d) sealed env credential-frei; (e) untrusted Egress hart gegated. Purer Parser unit-getestet (`stream-json-parse.test.ts`). Echter `claude`-Run = User-broker-approved go (deferred). |
| Backend-Wahl hinter einer Interface | **`registerSession({backend: "acp" \| "native"})`** (Default `acp` = deterministischer Stub) + **`createNativeTodoStarter`** (`TodoSessionStarter`-Seam, identisch zur ACP-Starter-Signatur) | angewendet (B8 P2a) | Akzeptanz „nativer Adapter als Backend hinter derselben Session-Interface wählbar". Default bleibt der Stub → Mock/Stub-Fallback + CI brauchen nie einen echten Agenten (Goldens/vitest/Playwright unverändert). Verifiziert (`native-backend-select.test.ts`): native Backend treibt den Run end-to-end durch das registrierte `todo`-Provider, broker-gegatet; Default bleibt ACP. |
| Architektur-Test | **`acp/claude-stream-exec.ts` als auditiertes `process`-Primitiv** in `EXECUTION_PRIMITIVES` | angewendet (B8 P2a) | Der einzige neue `child_process`-Spawn; sealed (wie `acp-transport.ts`). Broker-Chokepoint-Test bleibt grün. |

## ACP-Weg über die Zed-Bridge — initialize-Handshake (B8 P2b)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Handshake als neuer ACP-Verb | **`ACP_METHODS.initialize` + `ACP_PROTOCOL_VERSION = 1` + `AcpInitializeParams`/`AcpInitializeResult` (`contracts/acp.ts`)** | angewendet (B8 P2b) | Befund: die echte `@zed-industries/claude-code-acp`-Bridge (und jeder Standard-ACP-Agent) verlangt einen `initialize`-Austausch **vor** `session/new` — Client meldet die Protokoll-Version + dass er fs-Zugriff vermittelt (der Broker-Seam), der Agent antwortet mit der ausgehandelten Version + seinen Capabilities. Der in-repo-Stub spricht unser eigenes Protokoll und braucht das nicht. |
| Opt-in pro Session (Stub byte-identisch) | **`AcpSessionOptions.handshake` (Default `false`)** — `start()` ruft `initialize` nur bei `true`, fail-fast bei Versions-Mismatch den der Agent nicht erfüllen kann | angewendet (B8 P2b) | Hard-Rule „Stub-Fallback intakt": der Default-`acp`-Pfad bleibt handshake-frei → 357+ vitest + Stub + Goldens unverändert. Nur die Bridge-Auflösung setzt `handshake: true`. Durchgereicht über `createAcpTodoStarter({handshake})` und `registerSession({handshake})` (Test-Override) bzw. `RealAcpSpawn.handshake` (Auflösung). |
| Bridge-Spawn-Auflösung (kein roher Key) | **`resolveBridgeSpawn` (`acp/real-acp-config.ts`)**: `CAPISCO_ACP_CLI` → installierter Bin, sonst globaler `claude-code-acp`-Bin, sonst Fallback `npx -y @zed-industries/claude-code-acp`; immer `handshake: true`, **nie** key-gated | angewendet (B8 P2b) | Die Bridge übersetzt ACP ↔ Claude Code und nutzt den **bestehenden Claude-Login** → kein `credentialRef`, kein Schlüssel-Gate (anders als `resolveRealAcpAdapter`, das eine generische Key-tragende CLI auflöst). Die Auflösung **bereitet** nur den Spawn vor — sie fetcht nichts; der echte npx-Fetch/Bridge-Run ist der User-broker-approved go. Dormant (`cli-not-installed`) wenn weder Bin noch npx auflöst. |
| Sealed Spawn (unverändert) + kein neues Chokepoint-Primitiv | Die Bridge spawnt über den **bestehenden** `acp-transport.ts` (sealed env, piped stderr, client-assigned Taint) | angewendet (B8 P2b) | P2b führt **keinen** neuen `child_process`/`fetch`/`fs-write` ein — der Bridge-Spawn ist ein `{command,args}`-Swap hinter demselben stdio-Transport. Broker-Chokepoint-Architektur-Test bleibt grün (scannt nur `.ts`; der Fake-Bridge-Stub ist `.mjs`). |
| Verifikation (Fake-Bridge, kein echter Bridge-Run) | **`fake-acp-bridge.mjs`** — ein winziger Standard-ACP-Prozess der `initialize` **verlangt** (rejected `session/new` davor mit `before initialize`) und dann denselben gescripteten Broker-gegateten Flow fährt | angewendet (B8 P2b) | Hard-Rule: kein npx-Fetch, kein echter/bezahlter Claude-Call im Build. Beweist OUR Seite (`acp-bridge-handshake.test.ts`): (a) mit Handshake → `initialize` ausgehandelt, voller Run broker-gegatet end-to-end (`file-read`/`file-write` auditiert); (b) **ohne** Handshake → Bridge rejected `session/new` (`before initialize`) → der Run scheitert (Handshake ist load-bearing); (c) `resolveBridgeSpawn` löst Bin/npx auf, `handshake:true`, **kein** `credentialRef`. Echter Bridge-Run = User-broker-approved go (deferred). |

## AgentSettings — Backend-Picker (B8 P3)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Katalog additiv, bestehende API/CLI-Segmente erhalten | **AgentSettings behält `agent-settings-{api,cli}`-Segmente + Bodies; ein neuer `agent-settings-backends`-Abschnitt listet den Katalog darunter** | angewendet (B8 P3) | Bestehende Unit-/Visual-Specs (`agent-settings-api`/`-cli`, `/usr/local/bin/claude`) bleiben grün — der Picker ist eine additive Sektion, keine Ablösung. Pixel-Golden unverändert: das Settings-Popover ist Default zu (Landing-Golden unberührt). |
| Katalog-Quelle = deterministischer Snapshot (Stub-Default) | **`agentSnapshot.backends` (`AgentBackend[]`) + `defaultBackendId = "stub"` (`mocks/agents.ts`)** — Stub `ready` (selected), Claude-native `guide`, ACP-Bridge `installable`, Codex `guide` | angewendet (B8 P3) | Hard-Rule „Mock/Visual-Harness Default (Stub), Goldens byte-identisch": der Browser/Stub-Default rendert einen fixen Katalog (kein realer Host-Probe, kein `which`). Auf dem Desktop-Sidecar ersetzt das echte `provision.detect` die Quelle (dünner Swap). |
| Persistenz nur der Auswahl | **`useAgents` mit `persist` + `partialize` → nur `selectedBackendId` (Key `capisco-agents`), Default `stub`** | angewendet (B8 P3) | Akzeptanz „gewähltes Backend persistiert". Nur die Backend-Wahl überlebt Reload; Sessions/Run-States/Popover bleiben ephemeral. Default `stub` → CI/Goldens unbeeinflusst. |
| Aktionen pro Status (Install broker-gegatet, nie still) | **`ready`→Use/In-use (selektierbar, persistiert); `installable`→Install (surface das exakte broker-auditierte Kommando, kein Auto-Install); `guide`→Setup-Link** | angewendet (B8 P3) | Minimal-Friction-Flow: Use schaltet die Auswahl, Install zeigt das genaue Kommando das der Broker autorisieren würde (`agent-settings-install-gate`, „never silent") — der echte Install läuft über das sidecar-`provision.install` (broker-gegatet, deferred). Keine Aktion mutiert den Host aus der UI heraus. |
| i18n + testids | **Alle Strings über `t("agents.settings.*")`; `data-testid` pro Backend-Zeile + Status + Aktion (`agent-backend-<id>{,-status,-use,-install,-guide}`)** | angewendet (B8 P3) | Hard-Rule i18n/data-testid/Tokens. Unit (`AgentWorkspace.test.tsx`) + Visual (`agents.spec.ts`) decken den Picker DOM-seitig ab: vier Backends, Stub-Default „In use" disabled, ACP-Install surface das gegatete Kommando, native `guide`-Link. |

## LIVE Human-in-the-Loop Permission Gate (UI-approve/deny eines echten Agent-Runs)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Awaiting Resolver statt synchronem Default | **`PendingPermissionRegistry` (`sidecar/acp/pending-permission-registry.ts`)** — ist der `PermissionResolver`, parkt einen `ask` pro Session, gibt ein Promise zurück, das erst bei `resolvePermission(sessionId, requestId, decision)` über IPC resolved (fail-closed bei gebundenem Timeout / keinem Client) | angewendet | Befund (bestätigte Lücke): eine Live-Session hatte im Dev-Bridge keinen Resolver → Default DENY_ALL → Agent am ersten Tool-Gate blockiert. Der Resolver ist laut `acp-session §2` das EINZIGE was einen `ask` klärt — die Registry ist genau dieser Resolver, entscheidet aber nicht synchron, sondern surfaced den Gate an die UI und wartet auf die menschliche Entscheidung. |
| SessionId in den Resolver-Context | **`PermissionResolver`-Context um `sessionId` erweitert (`acp-session.ts` + `claude-code-provider.ts`)** — `#onAgentRequest`/`#onToolCall` reichen `this.#sessionId` durch | angewendet | Eine einzige Registry frontet alle Live-Runs über EINEN IPC-`agent`-Provider; das Per-Session-Keying ist der Context-`sessionId`. Additiv/non-breaking: bestehende Resolver (`() => ({axis:"session"})`) konsumieren das Feld nicht. |
| Live-Agent-Provider nur im Dev-Bridge (Unix-Sidecar bleibt Mock) | **`createLiveAgentProvider` (`sidecar/acp/live-agent-provider.ts`)**; `registerAllProviders(registry, { liveAgent })` swappt den `agent`-IPC-Provider NUR wenn `liveAgent` (Dev-Bridge setzt es); Unix-Sidecar lässt den Mock stehen | angewendet | Hard-Rule „Mock/Goldens byte-identisch": `ipc-integration.test.ts` pinnt den Unix-Sidecar-`agent`-Provider als deterministischen Mock (Parität + subscribe→done). Der Live-Swap ist Dev-Bridge-only — exakt wie der reale git/fs-Swap (`registerDevWorkspace`). Resolver-Wiring (`registerSession({pending})`) läuft auf beiden Flächen. |
| Broker-Invarianten erhalten | **`decide()` zwingt untrusted Egress weiter auf `ask`; die Policy-Engine clamped untrusted-egress-Resolution auf single-use `once` (target-gebunden)** — die Registry reicht die gewählte Achse nur durch | angewendet (unverändert) | Lethal-Trifecta §3.3 bleibt End-to-End: der Integrationstest beweist, dass ein zweiter gleich-geformter untrusted Egress RE-ASKt, obwohl der erste mit `once` geklärt wurde. Die Registry widened den Allowlist nie; nur eine Per-Call-Human-Decision (once/session/deny) fließt in den Broker zurück. |
| UI: Live-Prompt nur im Bridge-Pfad, Snapshot-Pfad unverändert | **`useLivePermission(sessionId)` + `LivePermissionBanner` in `Transcript.tsx`** — rendert nur wenn `isDesktop()` && ein `ask` geparkt ist; `onGrant(scope)` → `scopeToDecision` → `agent.resolvePermission` | angewendet | Hard-Rule: ohne Bridge (vitest + Playwright) ist der Hook inert (`pending: null`) → der gerenderte Baum ist byte-identisch der bisherige Snapshot-Pfad (84 Visual-Goldens unverändert, AgentWorkspace-Unit-Specs grün). Nur der Live-Pfad wird interaktiv. |
| Verifikation (kein echter/bezahlter Claude) | **`live-permission-gate.test.ts`** (IPC-Spine, `double-egress-stub-acp-agent.mjs`) + **`live-permission.test.tsx`** (UI-Klick → richtige Decision; deny → kein Side-Effect, Prompt clears) | angewendet | Hard-Rule: kein LLM-Key, kein realer Agent. Deterministischer Stub mit zwei untrusted Writes auf dasselbe Target beweist: `ask` surfaced als pending → `resolve(once)` unblockt genau eine Execution → zweiter gleich-geformter Egress re-askt; `deny` → beide Writes als `Edit (blocked)` protokolliert, nie ausgeführt. Broker-Chokepoint-Architektur-Test bleibt grün (neue Fixtures sind `.mjs`). |

## Local-Artifact-Hygiene (road-to-local-artifact-hygiene)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Projekt-lokaler Pfad (Phase 0) | **Ein Pfad `.capisco/`** (`sidecar/local/project-paths.ts`): `localArtifactPath()`→`.capisco/local/`, `cacheArtifactPath()`→`.capisco/cache/`; reine Konstanten + lexikalische Path-Resolution, kein I/O | angewendet | feature-gitignore.txt: „ein Pfad statt einer wachsenden Liste verstreuter Muster" → der Exclude-Eintrag bleibt eine feste Menge statt pro neuem Artefakt-Typ nachzupflegen. Keine projekt-lokalen Writes existierten heute (`recent-projects.json` ist maschinenweit unter `homedir`), also etabliert die Datei die Konvention; ein künftiger projekt-lokaler Write nutzt diese Resolver und fließt durch denselben broker-gegateten `file-write`-Chokepoint. |
| Persönlich-vs-geteilt (Phase 0 PO-Gate) | **Nur persönliche Unterpfade excludiert:** `CAPISCO_EXCLUDED_PATHS = [.capisco/local/, .capisco/cache/]`; `.capisco/project.toml` (geteilt) bleibt **versioniert** | angewendet | Default „nur persönliche Unterpfade". `isExcludedArtifact()` klassifiziert `local`/`cache` als excluded, `project.toml`/`src/*` als nicht — die Grenze ist getestet, nicht angenommen. `.capisco/` wird NIE pauschal ignoriert (sonst landete künftige geteilte Team-Config im Exclude). |
| `.git/info/exclude`-Primitive (Phase 1) | **`sidecar/git/git-exclude-exec.ts`** — audited `node:fs` read+write Home; `ensureExcludeBlockWrite` schreibt idempotent einen markierten Block (`# capisco — local, do not commit` … `# capisco — end`); upsert ersetzt einen stale Block in-place (Upgrade bei neuem Artefakt-Pfad), erhält alle Nicht-Capisco-Zeilen verbatim | angewendet | AK-G3: nur ein markierter Block, ausschließlich in `.git/info/exclude`, nie `.gitignore`. Idempotenz via `hasCompleteBlock` (alle required paths vorhanden → no-op). `resolveGitDir` unterstützt normales Repo (`.git/` dir) UND Worktree/Submodule (`.git`-Datei → `commondir`). Pfad symlink-aufgelöst (`canonicalRoot`, macOS `/var`→`/private/var`). Im `broker-chokepoint.test.ts`-Allowlist als `fs-read`+`fs-write`. |
| Broker-gegateter Exclude-Write (Phase 1, AK-G1/G2) | **`sidecar/git/git-exclude-broker.ts`** — `BrokerExcludeWriter.ensureExcluded(root)`: No-Repo + Idempotenz-Pre-Check OHNE Broker (kein spurious audit/ask beim zweiten Lauf), dann `file-write`-`authorize`→`ask`-Gate→`execute`; der Disk-Write läuft NUR in `broker.execute` | angewendet | AK-G1: mutierender fs-Write durch den Chokepoint (keine Read-Only-Allowlist; Default `file-write`=`ask`). AK-G2: der `ask` IST die einmalige sichtbare Bestätigung beim ersten Mal — nie heimlich in `.git/`. Deny-all-Gate → kein Disk-Change, kein `executed`-Audit. Trusted human intent (Capisco öffnet Projekt), kein `fromUntrusted` → Lethal-Trifecta-Gate greift nicht. C3 single-use grant wie `BrokerFsWriter`. |
| No-Repo (Phase 2, AK-G4) | **`resolveGitDir`→undefined** → `ensureExcluded` returnt `{hasRepo:false, wrote:false}` OHNE den Broker zu berühren; `.git/` wird NIE angelegt | angewendet | AK-G4: still nichts tun, kein Throw. Test: Temp-Dir ohne `git init` → kein `.git` entsteht, Audit-Log leer. |
| `core.excludesFile` respektiert (Phase 2) | **Read-only `git config --get core.excludesFile`** via die System-`git`-Primitive (`git-exec.ts`, `allowFail`), in `ExcludeOutcome.globalExcludesFile` durchgereicht — nur gelesen, NIE geschrieben | angewendet | feature-gitignore.txt: „respektiere eine vorhandene globale Ignore-Config". Der einzige `git config`-Spawn-Site bleibt in `git-exclude-broker.ts` (injectable für spawn-freie Tests), NICHT in der fs-Primitive → der Prozess-Chokepoint bleibt single-sourced über `git-exec.ts`. Doppelt-Abdecken (lokaler Block + globale Datei) schadet nicht; der Fall ist bekannt statt angenommen. |
| Verdrahtung | **Dev-bridge-only** in `register-dev-workspace.ts`: bei konfiguriertem `repo`+`broker` ruft ein `BrokerExcludeWriter` (session-clearing Resolver) `ensureExcluded(repo)` best-effort beim Boot; ein Deny / No-Repo blockt das Öffnen nie | angewendet | Wie der reale git/fs-Swap: der Unix-Socket-Sidecar behält die deterministischen Mocks (Daten-Contract unverändert → alle UI-Gates trivial grün, keine Goldens berührt). Hermetische Verifikation gegen `git init` Temp-Repos (`git-exclude.test.ts`, 16 Tests). |

## Token-Ökonomie (F3 — road-to-token-economy)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Memory-Kompression (P0) | **`src/lib/compress/`** — `caveman-ruleset.ts` (vendored, SPDX MIT, Attribution-Block) + `memory-compress.ts` (`compressMemory`): maskiert Code/URLs/Pfade hinter Sentinels VOR der Reduktion, restauriert byte-genau danach | angewendet (P0) | Das neue load-bearing Primitiv (Council: nicht das Meter). A-Invariante: schützenswerte Tokens byte-erhalten (wie caveman-compress) — `memory-compress.test.ts`. Reine Funktion, browser-safe (UI-Handoff + Sidecar teilen eine Impl), deterministisch. Regelsatz darf nur kürzen, nie Negation/Zahl/Identifier anfassen. |
| Handoff-Summary (P0→P1) | **`buildHandoffSummary(resumed)`** faltet die Transcript-Blocks zu einer komprimierten Carry-over-Zusammenfassung (Header benennt Provenance) | angewendet (P0/P1) | Speist den „neue Session mit Zusammenfassung"-Pfad — besser als Claude Codes leerer Neustart. Pure, deterministisch. |
| Rot→neue-Session-Handoff (P1) | **`src/shell/agents/handoff.ts` `buildSessionHandoff`** + Store `handoffToNewSession`; der Rot-Banner-`[New session]`-Button (`context-banner-new`) → `startHandoff`; Seed in der leeren Transcript-Sicht (`handoff-seed`) | angewendet (P1) | Verdrahtet das design-sync-v1-P4-No-op-Banner mit P0-Kompression. **Kein Auto-Switch** — nur per Button. **Parent wird nie mutiert** (B3 retry/copy Tamper-Disziplin): der Handoff verzweigt vorwärts. Handoff-Assert: `handoff.test.ts`. |
| Caveman-Injektion (P2) | **`sidecar/acp/caveman-terse.ts`** (vendored, SPDX MIT) `injectTerseDirective(prompt, config)` PREPENDED den Regelsatz als System-Kontext — injiziert in **beide** Backends: `acp-session.ts` (session/prompt) UND `claude-code-provider.ts` (sendUserPrompt); `terse` durch beide Starter + `registerSession` gefädelt | angewendet (P2) | „Einbauen" = Verhaltens-Spec injizieren, kein Skill-Install (Capisco besitzt den Client). Treffer auf **beide** `registerSession({backend})`-Pfade — der Backend-Wechsel ändert nie, ob terse greift. |
| Caveman-Default (P2) | **Default an, opt-out pro Session**; Store `terseEnabled/terseLevel/terseHintSeen` (persistiert); `ComposerBar` TerseControl (`composer-terse`) neben „reasoning effort"; einmaliger Hinweis (`terse-hint`) beim ersten Send | angewendet (Produkt-Entscheidung) | Opt-out trivial erreichbar; Einmal-Hinweis verhindert „kaputt/unhöflich"-Fehldeutung. Persistiert als Produkt-Präferenz; Default = frischer Boot → Goldens unberührt (nur die neue Composer-Control, agents/chat-Goldens neu generiert). |
| Caveman-Grenze (Invariante, AK-T3) | **Terse formt nur die *Erklärung*; Grenz-Flächen umgehen den Injektor STRUKTURELL.** Der Injektor ist eine reine Funktion über den AGENT-PROMPT-String; Broker/Audit/Quality/Secret/Commit-Pfade rufen ihn nie auf | angewendet (P2) | §4.3 Grounding + §3 Safety: Diagnostics/Broker-Prompts/Secret-Refs/Audit/Commits tragen den Regelsatz nie. Kein abschaltbarer Default — eine Invariante. **Caveman-Negativ-Assert** (`caveman-terse.test.ts`: Audit-Records tragen den Marker nie, beide Backends). Positiv-Assert: `sentSystemContext`-Getter, Assert gegen den String (kein LLM-Roundtrip). |
| RTK-Exec-Primitive (P3) | **`sidecar/rtk/rtk-exec.ts`** — `execFile` (kein Shell, argv-Array; raw→stdin, compressed→stdout), `resolveRtkPath`/`rtkAvailable`/`rtkCompress`. Bounded timeout. Im `broker-chokepoint.test.ts`-Allowlist als `process` | angewendet (P3) | B1-Git-Disziplin (execFile, kein Shell). RTK ist broker-relevant (untrusted external binary im Datenpfad, AK-T5), aber READ-shaped (text→text). Realer Binary-Install (brew/cargo/curl) = User-broker-approved (deferred); `rtk-fixture-filter.mjs` exerziert den Spawn-Pfad deterministisch. |
| RTK-Scope (P3) | **`rtk-compressor.ts` `compressObservation(raw, {source})`** — nur `*-longtail`-Sources (`shell`/`ls`/`find`/`docker-ps`); `authoritative` wird outright refused (throw) | angewendet (P3) | Nur LLM-Observation-Pfad, nur unstrukturierter Long-Tail. git (B1) + quality (B5) parsen typisiert → bleiben autoritativ; RTK ersetzt sie nie. Eine Grenzfläche kann nie durch RTK geroutet werden (struktureller Gate). |
| RTK-Trust-Grenze (P3, AK-T1/T2) | **`contracts/rtk.ts`** — RTK-Output ist `LlmFacingOnly` (phantom-gebrandeter String → nicht zu `string` zuweisbar, Compiler-Gate) + Laufzeit-Marker (`RTK_FILTERED_MARKER`); `capability-broker.ts` `authorize` + `audit-store.ts` `record` **refusen** den Marker → Einbahn-Datenfluss strukturell | angewendet (P3) | Eine externe Kompression im Datenpfad darf die Fakten nie anfassen, auf die Broker/Audit sich verlassen. Doppelt abgesichert: TS-Brand (compile) + Laufzeit-Refusal (defense-in-depth). **RTK-nie-im-autoritativen-Pfad-Assert** (`rtk-observation.test.ts`). |
| RTK-Degrade (P3) | **Fehlt RTK → `rtkCompress` gibt `undefined`** (nie throw); `compressObservation` reicht den Rohtext durch (branded, `compressed:false`), kein Hard-Fail | angewendet (P3) | Sauberer Degrade. Der Degrade-Pfad sieht nur eine Long-Tail-Observation (authoritative ist vorher refused) → keine Grenzfläche durch Ersatzlogik. **RTK-Degrade-Assert** (`rtk-observation.test.ts`). |
| RTK harte-Dep-vs-Angebot (PO-Gate) | **Angebot** (clean degrade bei fehlendem Binary), kein Bundling | angewendet (Default) | token-economy-decisions §Offene. Realer Install deferred (User-broker-approved). |
| Determinismus | **RTK** Parser golden gegen eingefrorene Long-Tail-Fixtures (`rtk-parse.ts` `parseLongTail`, `fixtures/ls-longtail.{raw,expected}.txt`, idempotent); **Caveman**: getestet wird die **Injektion**, nicht der terse Output; **Ampel-Klasse** statt Token-String | angewendet | RTK ist deterministisch (Output-String→kompakt). Caveman ist LLM-Verhalten → nicht golden-testbar; Test pinnt die Injektion + dass Grenz-Flächen sie nicht tragen. |

## Modell-Routing (F5 — road-to-token-economy Phase 4)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Routing-Achse | **Nach Herkunft/Rolle (deterministisch), NIE nach Inhalt** — `contracts/model-routing.ts` (`SessionOrigin`/`ModelTier`/`RoutingDecision`) + `lib/model-routing/router.ts` `routeOrigin(origin)` (reine Funktion) | angewendet (P4) | feature-model-switch.txt + Council: ein Inhalts-Classifier ist nicht-deterministisch und bräuchte fast das große Modell, um die Modellwahl zu treffen. Capisco kennt die Herkunft strukturell (der Orchestrator hat die Session so gespawnt) → reine, deterministische, unit-testbare Funktion (`router.test.ts`, 16 Tests). |
| Tiers + Default | **small/mid/large**; mechanisch (roadmap-step mechanical/test-writing/doc, subagent fixer/test-writer/summarizer/searcher, todo) → small; analysis → mid; **free-conversation/architecture/design NICHT geroutet** (Default large) | angewendet (P4) | Asymmetrie: unterschätztes Haiku = „fast richtig" = der Marktschmerz. Bei freier Konversation/Architektur ist das Fehlklassifikations-Risiko am höchsten + der mechanische Anteil am kleinsten → gar nicht erst routen. |
| Quality-getriebene Eskalation | **`escalateOnQuality(decision, qualityFailed)`** — klein-zuerst; B5 rot → genau eine Stufe hoch (gecappt bei large), Fehler als Kontext | angewendet (P4) | Kein vager Confidence-Score: das geerdete B5-Quality-Urteil IST das Router-Feedback (Capisco-eigene Antwort). Eskalation downgradet nie + behält das Sperrlisten-Flag. |
| Sperrlisten-Invariante | **`isBlocklisted(origin)`** — broker-decision / ai-review / untrusted-egress / reviewer-subagent → IMMER large, nie heruntergestuft | angewendet (P4) | Invariante, kein Default (gleicher Geist wie Prod-Read-only). Eine Berechtigungs-/Urteils-/Safety-Entscheidung von einem schwächeren Modell wäre ein Downgrade des Schutzes. |
| on/off-Default | **DEFAULT AUS** (`routingEnabled: false`, persistiert); Toggle in der Composer-Control-Bar (`composer-routing`) | angewendet (Produkt-Entscheidung) | Greift non-deterministisch ins Ergebnis ein → erst an realen Roadmap-Läufen kalibrieren, bevor es still Modelle tauscht. Council-Empfehlung: hier default aus (anders als Terse). |
| Sichtbarkeit + Override | **Modell-Badge pro Session-Knoten** (`SessionTabbar` ModelBadge → `effectiveModel`) + **Per-Session-Mensch-Override** (`ComposerBar` Override-Picker, Store `modelOverrides` persistiert); Override gewinnt immer, „Auto" löscht ihn | angewendet (P4) | Auto-Routing, das unsichtbar passiert, zerstört Vertrauen wenn es danebenliegt. Der Mensch sieht das effektive Modell + kann pro Session überstimmen. Default (Routing aus, keine Overrides) = unverändertes Badge-Rendering → Goldens nur wegen der neuen Composer-Control neu generiert (agents/chat). |
| Ersparnis-Realismus | **Ersparnis real, aber kleiner als das Bauchgefühl** — Eskalationspfad zahlt den kleinen Versuch obendrauf; Klasse-C-Kalibrierung an realen Läufen offen | dokumentiert | Reasoning-Tokens dominieren oft; Netto-Gewinn nur wenn die Mehrheit der gerouteten Aufgaben wirklich mechanisch ist. |

## Cross-Projekt-Wissen (F4 — road-to-cross-project-knowledge)

### Phase 1 — `@projekt`-Verweis (autonom, Klasse A)

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Mention-Query-Primitive | **`src/lib/mention/mention-query.ts`** — pure, browser-safe: `activeMention(text,caret)` (Caret-im-`@token`), `matchProjects(projects,query,exclude)` (substring auf NAME, rank by deterministischem `lastSeen`-Ordinal), `insertReference(text,mention,project)` | angewendet (P1) | Reiner Kern, unit-getestet ohne DOM (`mention-query.test.ts`, 13 Tests, Seed-Registry). Autocomplete über Projekt-NAMEN, nie Methoden/Symbole (Symbol-Autocomplete ist explizit eine spätere Phase — feature-ide-linking.txt Runde 3). `@` triggert nur am Bufferstart / nach Whitespace → `email@host` triggert nie. |
| Mention-UI-Primitive | **`src/shell/agents/MentionAutocomplete.tsx`** — self-contained Input-Wrapper (forward-ref), liest die Registry **lazy** (erst bei Interaktion, nie beim Mount → keine Arbeit in der Visual-Harness); Overlay-Listbox erscheint **nur** bei aktivem `@`-Token; Arrow/Enter/Escape-Nav | angewendet (P1) | „@projekt-Autocomplete in Markdown-/Notiz-/ToDo-Kontext". Self-contained + komponierbar (wrappt das `Input`-Primitiv verbatim → selbe testid/Klassen). `role="combobox"`+`aria-autocomplete="list"`+`aria-expanded` (axe: ein nacktes Input darf kein `aria-expanded` tragen). Liste mountet nie im Default-Boot → Pixel-Goldens byte-identisch. |
| Klickbarer Verweis | **Auswahl fügt eine kanonische `@name `-Referenz ein (kennt den Pfad)** + ruft den vorhandenen „Projekt öffnen"-Pfad (`useOpenProject.open`) — mittlere Lesart „Klick öffnet das Projekt / springt dorthin" | angewendet (P1) | Der Verweis ruft nur den existierenden Open-Pfad auf (kein neues Subsystem). In den Composer (Agents + Chat) verdrahtet — der nächstgelegene Markdown-/Notiz-Kontext. |
| Veraltete Pfade leise | **`onOpenReference` resolved `false` für einen Pfad, der nicht mehr existiert** → soft `mention-stale`-Note (role=status), **kein** toter Verweis, **kein** Error-Spew | angewendet (P1) | feature-ide-linking.txt Runde 3 Vorbehalt: „leises ‚existiert nicht mehr' statt totem Verweis". Der Store schluckt einen fehlgeschlagenen Open in sein `error`-Feld; `openReference` liest das + meldet Staleness ruhig. |
| Verifikation (DOM/Routing) | **`MentionAutocomplete.test.tsx`** (5 Tests, Seed-Registry): keine Liste vor `@`; Treffer aus der Registry (current excluded); Referenz-Insert + Open-Flow gefeuert; Stale-Pfad → ruhige Note; Arrow+Enter-Nav | angewendet (P1) | Gegen den Default-Mock-Seed (capisco/core-api/design-system, deterministischer Ordinal-`lastSeen`). Kein Bridge/echter Agent nötig. |

### Phase 2 — Cross-Projekt-Session-Brücke (HUMAN-GATED, Voll-Lethal-Trifecta)

> **Decision-Gate bleibt beim Menschen:** Die CONTROLS sind gebaut + gegen Fakes verifiziert; der **live Cross-Projekt-Cloud-Egress bleibt ein deferred, expliziter User-Go** (kein Auto-Feuern, Default fail-closed deny-all). Zwei Beine brechen bewusst redundant: **Egress-Human-Gate (AK-C3)** UND **Redaction/Quarantäne (AK-C1+C2)**.

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Neue Scope-Achse `cross-project-read` (AK-C4) | **`CapabilityKind` um `"cross-project-read"` erweitert** + Default-Grant-Regel `{ kind:"cross-project-read", pattern:"*", verdict:"ask" }` — **nie `allow`** in der Default-Config | angewendet (P2) | Eine neue Scope-Achse, fail-closed: ohne explizite Regel → `ask` (Engine-Default); die explizite `ask`-Regel dokumentiert, dass ein `allow` hier per Design verboten ist. Nicht in der Default-Allowlist. Mensch autorisiert den Read pro Quell-Projekt. |
| Persistenter Cross-Projekt-Store (Voraussetzung) | **`ProjectStoreEntry`-Föderation** (path-gekeyt, ein `SessionStore` pro Projekt-Root) + `createProjectStoreFederation` (defensive copy) | angewendet (P2) | Der heutige Store ist pro-Store/in-memory; Cross-Projekt-Read braucht persistent + projekt-übergreifend lesbar. Die Föderation ist die Naht; der echte Swap = disk-backed Store pro Root hinter demselben Contract (dünner Provider-Swap, keine Brücken-Änderung). |
| Redaction/Inject-Stufe (AK-C1, Quarantäne-Bein) | **`sidecar/cross-project/redact-excerpt.ts`** — `carriesSecret(text)` **refuses** value-shaped Secrets (`password=`/`token:`/`Bearer …`/PEM/high-entropy-Assignment), `redactToExcerpt` dropt den Block statt ihn durchzulassen | angewendet (P2) | Tresor-Disziplin auf dem Brücken-Pfad (gleiches Muster wie Audit/Datasource-`credentialRef`). Loud refuse, kein Soft-Scrub. Bewusst NICHT `looksLikeSecretValue` (dessen Long-Blob-Heuristik flaggt Prosa false-positive); nur die diskriminierenden Shapes. |
| Redaction läuft auf dem VOLLEN Block-Body | **Die Brücke `resume`t die Session + redaktet den vollen Block-Body, NICHT das vorab-getrimmte Store-Snippet** | angewendet (P2, Befund während des Baus) | Ein Snippet-Fenster könnte das `=`/`:` eines Secrets ausschneiden und den Wert lecken (real beobachtet: `…h0rse-battery-staple to the db` rutschte durch). AK-C1 entscheidet auf dem ganzen Body; AK-C2 schneidet das Fenster erst NACH dem Secret-Check. |
| Kuratierte Auszüge, nie Volltext (AK-C2) | **`CrossProjectExcerpt`** trägt nur Provenance + einen kurzen kuratierten `snippet` (≤160 Zeichen); **kein** Volltext-Getter über die Projektgrenze | angewendet (P2) | Ein 40-Nachrichten-Verlauf würde B's Kontext zumüllen UND ist der Leak-Vektor. B bekommt nur Auszüge/Zusammenfassungen. |
| Egress-Gate vor Cloud (AK-C3, Egress-Bein) | **`injectIntoPrompt` = `network` + `fromUntrusted:true`** → Broker forciert `ask` (nie auto, nicht von session/scoped vorab-freigebbar); nur eine **Per-Call-Human-Entscheidung** sendet, als `human`-Principal über die **trusted** Request-Form (gelaundert wie `TicketLifecycleImpl`); sonst `gated`, nichts verlässt die Maschine | angewendet (P2) | Broker §3 MUST-NOT 4 / Lethal-Trifecta. Default deny-all (live Cloud-Egress deferred). `performEgress` = No-op (echter HTTP-Adapter deferred, Key im Execution-Layer). |
| Menschgesteuerte Relevanz (AK-C5) | **`searchProject(path,query)` / `curateFromSession(path,sessionId,query)`** — Caller benennt Quell-Projekt + Session explizit; **keine** „search-every-project"-Methode, kein Auto-Fan-out | angewendet (P2) | Anfangs explizit-manuell („zieh aus DIESER A-Session"). Auto-Relevanz ist ein späteres, schwereres Feature. |
| Wissen ≠ Zugriff (AK-C6, harte Invariante) | **Die Brücke hält KEINEN Worktree-/fs-/shell-/Container-Handle für das Quell-Projekt; Output ist `CrossProjectExcerpt` (reiner Text)** | angewendet (P2) | Kein Codepfad erlaubt B's Agent, aus einem A-Auszug eine Operation auf A's Dateien/Containern auszulösen. Cross-Projekt-*Zugriff* (ausführend) bleibt eine eigene, spätere broker-gescopte Capability (out of scope). |
| Chokepoint-Architektur-Test grün | **`broker-chokepoint.test.ts` bleibt grün** — die neuen Brücken-Dateien tragen keine Side-Effect-Tokens (`fetch(`/`child_process`/fs-write); der Egress ist ein No-op-Callback | angewendet (P2) | Der echte Cloud-HTTP ist deferred + würde im allowlisteten Execution-Primitiv leben, nie in der Brücke. Kein neues unmediated Egress eingeschleust. |
| Verifikation (Pflicht-Tests, gegen Fakes) | **`cross-project-bridge.test.ts`** (18 Tests): Redaction refuse/curate; Read-Gate fail-closed (keine Auszüge ohne Human-Grant) + Audit-vor-Read; **Cross-Projekt-Auszug-secret-frei** (Negativ-Assert über die Grenze, Secret-Block gedroppt); deterministischer Hit-Set (monotoner `seq`); Egress-Gate (deny→gated/nichts gesendet, human-clear→sent als `human`/trusted, session-Grant kann späteren Egress NICHT vorab-freigeben); AK-C6 Text-only; Föderations-Defensive-Copy | angewendet (P2) | Search über Seed-Sessions zweier Mock-Projekte (frontend/backend), deterministisch. Kein Bridge/echter Agent/echter Cloud-Call. |

## Modell-Routing (road-to-model-routing P0/P1)

> **Decision-Gate (PO):** Feature **default AUS** (anders als Terse) — es greift
> non-deterministisch ins *Ergebnis* ein und wird erst an realen Roadmap-Läufen
> kalibriert (Klasse C). Nur **mechanische Klassen** werden geroutet; freie
> Konversation/Architektur **nie**. Reale Ersparnis = menschliches/echtes Gate,
> daher **kein Archivieren** des Roadmaps bei `count_open==0`.

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Live-Router-Komposition vs. reine Funktionen | **`sidecar/model-routing/live-router.ts` (`LiveModelRouter`)** komponiert die bereits getesteten reinen Funktionen (`routeOrigin`/`escalateOnQuality`/`isBlocklisted`/`resolveModel` aus `src/lib/model-routing/router.ts`) — fügt NUR den on/off-Schalter + den Laufzeit-Invarianten-Check hinzu, dupliziert keine Routing-Logik | angewendet (P0) | „auf Vorhandenem aufbauen, komponieren, keine Duplikation". Die reine Schicht bleibt der deterministische, erschöpfend unit-getestete Kern; der Live-Router trägt die zwei zustandsbehafteten Belange (Toggle, Tier→Modell-Binding) und nichts sonst. |
| on/off-Schalter, default AUS | **`LiveModelRouter({ enabled })` default `false`**; bei AUS bleibt jede (nicht-blocklistete) Session auf der Default-Stufe **`large`** | angewendet (P0) | Routing schrumpft nur je von `large` nach unten — „aus" === „immer das stärkste Modell" === die konservative Posture. Fehlendes Flag wird als aus behandelt. |
| Sperrliste als LAUFZEIT-Invariante (nicht nur Prädikat) | **`decide()` prüft `isBlocklisted` STRUKTURELL VOR dem Toggle** → Broker-Pfad/KI-Review/untrusted-egress/`reviewer`-Subagent immer `large`, **unabhängig vom Schalter**; **kein** Setter für die Sperrliste auf der Klasse | angewendet (P0) | Eine Berechtigungs-/Urteils-/Sicherheits-Entscheidung von einem schwächeren Modell = Sicherheits-Downgrade. Der Toggle kann diese Flächen strukturell nie erreichen. Sperrliste erweitern = Security-Entscheidung, nie autonom (daher kein API-Setter). |
| Verdrahtung in den Spawn-Pfad | **Beide ToDo-Starter (`createAcpTodoStarter`/`createNativeTodoStarter`) + `registerSession` nehmen optional `router` + `origin`**; das Spawn-Modell = `model ?? router.resolveSpawn(origin).model ?? <fixer Default>` — einmal bei Konstruktion aufgelöst (Origin fix → Modell fix, deterministisch) | angewendet (P0) | Der Orchestrator kennt die Herkunft (er spawnt den Knoten). Ein expliziter `model` (Test-/Human-Override) gewinnt immer. Ohne Router bleibt der pre-routing Default → bestehende Verdrahtung unverändert (opt-in). |
| Tier→Modell-Binding ist Config | **`DEFAULT_TIER_MAP = { small:"Haiku 4.8", mid:"Sonnet 4.8", large:"Opus 4.8" }`** (die Composer-Labels); überschreibbar via `tierMap` | angewendet (P0) | Dieselbe Routing-Logik treibt beliebige Deployment-Bänder; die Stufe ist die Abstraktion, das Modell die Bindung. |
| Quality-getriebene Eskalation (B5 als Router-Feedback) | **`sidecar/model-routing/escalation.ts` (`runWithEscalation`)** — klein-zuerst, auf RED-Quality-Urteil eine Stufe hoch mit den **Fehlern als Kontext** (`buildEscalationPrompt`), bis GREEN oder `large`-Decke; Run-Seam (`RunSession`) + Gate-Seam (`QualityGate`) injiziert | angewendet (P1) | Die eleganteste Antwort ist nicht „einmal richtig raten", sondern „klein zuerst, eskaliere auf hartem Signal". Das RED-Quality-Urteil ist ein deterministisches, geerdetes Faktum (kein Confidence-Score). Eskalation übersteigt nie `large`, downgradet nie; eine blocklistete Herkunft startet bei `large` → Eskalation ist strukturell ein No-op. |
| KEIN echter LLM / kein echter Eskalations-Cost im Test | **Run = deterministische broker-gegatete Stub-Session; Urteil = FAKE RED/GREEN-Verdict** | angewendet (P1) | Eskalation wird gegen ein erzwungenes Fake-Urteil + den Stub-Agent verifiziert — fires-on-red (trägt die Fehler), fires-NOT-on-green. Keine reale (bezahlte) Eskalation. `verdictFromResults` ist die reine Falte für den späteren echten B5-Runner-Pfad. |
| Doppel-Lauf-Kosten transparent | **`EscalationOutcome.attempts[]`** listet jeden Lauf (Stufe + Modell + Verdict) + `escalated`-Flag | angewendet (P1) | Klasse-C-Realismus: der unterschätzte Fall läuft DOPPELT (klein gescheitert, dann groß) — der kleine Versuch kommt obendrauf. Netto-Gewinn nur, wenn die Mehrheit der gerouteten Arbeit mechanisch ist; die Sichtbarkeit macht das auditierbar, nicht versteckt. |
| Chokepoint-Guard grün (Naht-Benennung) | **Run-Seam heißt `RunSession`/`runSession`, NICHT `spawn`** | angewendet (P1, Befund während des Baus) | `broker-chokepoint.test.ts` matcht `\bspawn\s*\(` als Prozess-Spawn-Token. Der Seam ist eine funktions-typisierte Naht (kein echter Subprozess) — Umbenennen (nicht Allowlisten) hält die Sicherheits-Invariante intakt, statt sie aufzuweichen. |
| Benigne Teardown-Stream-Errors geschluckt (Bugfix) | **`AcpTransport` hängt `error`-Listener an stdin/stdout/stderr + den Child**; der fire-and-forget `prompt`-Request in `AcpSession` bekommt `.catch(()=>{})` | angewendet (P1, Bugfix den die parallelen Spawns aufdeckten) | Beim `close()` (`stdin.end()`+`kill()`) emittiert eine in-flight-Pipe ein ASYNC `error`-Event (EPIPE), das ein synchrones try/catch nicht fangen kann; `#onClose` rejected pending-Requests („ACP agent process closed"). Unter paralleler Last (70 Test-Dateien) tauchten diese als unhandled errors/rejections auf (vitest flaggt sie als mögliche false positives — alle Assertions blieben grün). Reine Teardown-Robustheit, keine Verhaltens-/Security-Änderung; verifiziert: 5× voller Suite-Lauf sauber. |
| Sperrlisten-Invariante durch den ECHTEN Spawn-Pfad (P2) | **`live-routing-spawn.test.ts` negativ-asserted die VOLLE Sperrliste** (broker-decision · ai-review · untrusted-egress · `reviewer`-Subagent) durch den realen broker-gegateten Stub-Spawn (das *gespeicherte* Session-Modell), mit Routing AN **und** AUS, plus Toggle-Flip on→off→on über Spawns | angewendet (P2) | Die Invariante wird strukturell an der Spawn-Grenze bewiesen, nicht nur als reines Prädikat (`live-model-router.test.ts` deckt die reine Schicht). Kontroll-Assert: derselbe AN-Router routet eine mechanische ToDo auf `small` (Haiku) — beweist, dass das `large` der Sperrliste die Invariante bei der Arbeit ist, nicht „Routing aus". Kein API-Setter für die Sperrliste → Erweiterung ist eine Code-/Security-Entscheidung, nie autonom zur Laufzeit. |
| Modell-Badge pro Session-Tree-KNOTEN (P3) | **Subagent-Chips (`SubagentRow`) tragen jetzt eine `ModelBadge`** (`subagent-model-<id>` testid), wiederverwendet aus `components/capisco/model-badge.tsx`; die Session-Tab-Badge (Parent-Knoten) zeigte `effectiveModel` bereits seit P0 | angewendet (P3) | „Welches Modell macht welche Aufgabe" gilt pro Knoten, nicht nur pro Session — ein Subagent IST ein Tree-Knoten (`SubAgent.model`). Dieselbe Badge-Komponente, kein Hex (Tokens via `Badge`-Outline-Variante). Kein Override-Surface am Subagent (er erbt die Orchestrator-Routing-Entscheidung; der Human-Override ist die Per-Session-Kontrolle am Parent-Tab). |
| Mock-Subagent-Modell (P3, bewusste Daten-Änderung) | **`s1a.model` `"Claude"` → `"Haiku 4.8"`** | angewendet (P3) | Der Showcase soll die Routing-These zeigen: ein umschriebener „write tests"-Subtask wird auf die SMALL-Stufe geroutet (Parent Opus/Claude, Subtask Haiku) → die Badge demonstriert sichtbar „klein wo möglich". Pixel-Delta blieb unter `maxDiffPixelRatio 0.01` → **`agents-dark`/`agents-light`-Goldens byte-identisch, keine Neugenerierung**; nur DOM/testid-Asserts (Unit + Playwright) hinzugefügt. |
| Per-Session-Override (P3, bereits seit P0 vorhanden) | **`modelOverrides` im Agents-Store + `OverridePicker` in `AgentSettings`** (`agent-settings-routing-override*`); `effectiveModel(session, overrides)` = Override gewinnt, sonst eigenes Modell; persistiert | angewendet (P3) | Der Mensch überstimmt pro Session („das hier mit Opus") — DOM-asserted (Override setzen → Badge flippt → „Auto" löscht). Unsichtbares Auto-Routing vermeiden. Toggle `routingEnabled` default AUS, DOM-asserted. |
| **UI = Design-System-CSS VERBATIM, KEIN Tailwind-Nachbau** (design-sync-v2, 2026-06-23) | **Design-Flächen werden 1:1 aus dem Prototyp übernommen: die Design-System-CSS liegt verbatim in `src/styles/capisco-composer.css` (`--ds-*`-Tokens [:root=Light/.dark=Dark] + `.cmp*`/`.ab-*`/`.as-*`/`.bk-*`/`.trow-*`/`.cb-*`/`.ctx-*`/`.tune-*`-Regeln, Token-Refs auf `--ds-` umpräfixt), die Komponenten tragen die EXAKTEN Prototyp-Klassen + Markup.** Tailwind-Utilities werden NICHT genutzt, um Prototyp-Designs nachzubauen. | angewendet (design-sync-v2) | **Iron Rule (Nutzer, mehrfach): kein eigener Code, keine freie Interpretation, kein Tailwind-Approximieren des Designs.** Jede Tailwind-Übersetzung des Prototyps wich ab (Auto-Button-Maße, Fonts/px-Größen, Override-Dropdown, ActivityBar 48px statt 42px, „Dock"-Text, lucide-Revert statt Entwurfs-SVG); jeder Verbatim-CSS-Port (Composer, ActivityBar, Agent-Settings) passte. Quelle: `agents/tmp/design-system/ui_kits/capisco-ide/` (`index.html`-CSS + `*.jsx`-Markup) + `tokens/*.css`. Neue/zu matchende UI: Prototyp-CSS in `capisco-composer.css` übernehmen + Prototyp-Klassen verwenden — NICHT in Tailwind nachbauen. Bestehendes Tailwind in reinen shadcn-Primitiven/Layout-Glue bleibt; Design-Flächen sind CSS-verbatim. |

## Desktop-Release (road-to-desktop-release P0)

> **Kontext:** Aus dem Tauri-Shell ein verteilbares Bundle machen. Der Sidecar trägt die
> Security-Invarianten (Broker-Chokepoint, `claude`/ACP-Subprozess-Spawn, Unix-Socket,
> Secret-Injektion) — daher sind die Runtime- und Transport-Entscheidungen hier
> Foundation-Gates. Council-frei, mit Matze pro Gate abgenommen (2026-06-25).

| Gate | Entscheidung | Status | Begründung |
|---|---|---|---|
| Sidecar-Single-Binary-Bundler | **Bun-compile** (`bun build --compile sidecar/main.ts --target=bun`), NICHT Node-SEA/`pkg` | angewendet + verifiziert (P0) | Ein Befehl, Bun schon installiert (1.3.10), kompiliert TS + `@/`-Aliase + ESM nativ und bettet importierte Assets ein. Node-SEA bräuchte eine esbuild→SEA-Config→postject-Pipeline; `pkg` ist archiviert (schwaches ESM/Node-25). Parität (Dev↔Prod) ist über den Runtime-Split unten gesichert, nicht über den Bundler. Verifiziert: 58 MB Mach-O arm64, bootet mit **Node-freiem PATH**, bindet den Unix-Socket, bedient `listSessions` über die Wire. |
| Runtime-Split (Parität dort, wo sie zählt) | **Prod-Sidecar + Sidecar-Tests = Bun; Dev-Sidecar-Prozess + dev-only WS-Bridge = Node** | angewendet (Bun-Test-Baseline) / Test-Split + 3 Tweaks offen (P0) | „Ship-what-you-test": das ausgelieferte Artefakt **und** seine 43 Security-Test-Dateien laufen unter Bun. Die WS-Dev-Bridge bleibt Node, weil Buns `node:http`-`upgrade`→Raw-Socket den handgeschriebenen 101-Handshake **nicht ausliefert** (empirisch: server `'upgrade'` feuert, Client-101 kommt nie an → 5 s Timeout). Ein `Bun.serve`-Zweig wäre ein zweiter Pfad für „ein" Ziel; die WS-Bridge ist Nicht-Prod-Transport + Wegwerf-Plumbing, also bleibt sie auf Node. Baseline: **415/424** Sidecar-Tests grün unter `bun test` out-of-box; offen: 3 Negativ-Pfad-Tests (Async-Fehlerpropagation B+C) + 6 WS-Bridge-Tests (bleiben auf vitest/Node). |
| Prod-Webview↔Sidecar-Transport | **Tauri-IPC-Relay** (Rust pipet NDJSON 1:1 zum Unix-Socket des gespawnten Sidecars), NICHT Loopback-WS in Prod | entschieden, **noch nicht implementiert** (P0) | Kein lauschender Port im Auslieferungs-Build → engste Trust-Boundary für den Broker-Transport. Verträgt sich mit dem Cancel des „bespoke Tauri-IPC-Byte-Channel" (der verbot ein **IPC-Stack-Duplikat** in Rust; ein dünnes Byte-Relay dupliziert den getesteten Stack nicht). Die WS-Bridge bleibt dev-only/loopback wie im Trust-Vertrag. Offen: Rust-Spawn + Relay-Commands (lib.rs) + Frontend-`TauriIpcTransport` (non-DEV-Pfad). |
| Fixture-Embedding (Bundle-Boot-Fix) | **`load-fixtures.ts`: Laufzeit-`readFileSync(fixtures/*.json)` → statische JSON-Imports** (`with { type: "json" }`) | angewendet + verifiziert (P0) | `bun build --compile` bettet nur den JS-Graph + **importierte** Assets ein, keine Laufzeit-Datei-Reads → das Binary crashte beim Boot mit `ENOENT /$bunfs/root/fixtures/jira.task.json`. Statische Imports werden ins Binary eingebettet. On-Disk-Userdaten-Reads (fs/git/recent-projects) bleiben `readFileSync` (lesen echte Dateien zur Laufzeit, korrekt). Verifiziert: 424 vitest grün (Node) **und** Binary bootet (Bun). |
| Build-Verdrahtung | **`scripts/build-sidecar.mjs`** (Triple-Name via `rustc -vV`-`host:`) → `src-tauri/binaries/capisco-sidecar-<triple>`; `bundle.externalBin: ["binaries/capisco-sidecar"]`; `task release:sidecar` + `task release:build` (deps → sidecar → `tauri build`); `binaries/` gitignored | angewendet + verifiziert (P0) | `externalBin` hängt den Target-Triple selbst an → das Build-Script muss exakt so benennen. 58-MB-Artefakte gehören nicht ins Repo. `tauri build` selbst (bettet das Binary ins `.app`) + der Display-Smoke „öffnet ein Projekt" sind noch nicht ausgeführt (teils Klasse-C). |
| Packaged-Agent-Default (Follow-up, NICHT P0) | **offen** — der Default-`acp`-Stub spawnt `stub-acp-agent.mjs` via `node`, was im gepackten Binary fehlt | offen (Follow-up) | Im gepackten Build ist das sinnvolle Backend `native` (`claude`-Login) oder eine echte ACP-CLI, nicht der In-Repo-Stub. Den Packaged-Default + das Spawnen externer Agenten ohne Host-`node` regelt eine spätere Phase; der P0-Smoke (Projekt öffnen) exerziert den Agent-Spawn nicht. |

